# Paper continuation, 2026-09-15

## Coordination checkpoint after E-0399: Shop-first proposal, not execution approval

Overlord requested this durable checkpoint and bounded ownership/measurement
coordination. No new model, experiment, runtime selection, accepted amendment,
manuscript work, integration or commit is authorized by that request. Master
remains 1.5.47; the ledger remains E-0399. No paper model run is in flight.

Observed checkout: /Users/luis/Projects/malleus-dev, branch main, HEAD
18015352e2eb5bffbb58125c95de40eba0f4c992. This is a dirty shared checkout,
not the frozen experiment runtime. Preserve all existing changes. Relevant
uncommitted resume files are this handover, paper-v4/paper-master-plan.md,
paper-v4/paper-ledger.md, paper-v4/AGENTS.md,
paper-v4/experiment-v4/RELATIONSHIP-REPAIR-RESULTS.md and
paper-v4/experiment-v4/test_relationship_repair.py. The experiment-v4
relationship/preparation harness and tests also exist as untracked files.
Numerous other paper, manuscript, submission and research edits predate this
checkpoint; they are not a staging list or evidence of work done by this turn.
Do not stash, reset, delete, stage broadly or move refs to make this look clean.

The earlier E-0399 text saying KGs' representation is unselected describes that
historical boundary. Luis subsequently selected B, separating estimate origin,
citation and later application. The concrete KGs schema remains proposed, not
an accepted ontology or population. Its corrected draft is
private/kg-representation-recon-2026-09-15/ge-1.proposed.yaml,
sha256:78cbe173e6c0ae21019f773657ac1dece026aa97bb2a1d0f1239683dd02848d3.
Four previously run static declaration tests do not establish compilation,
populated graph behavior, reader success or model acquisition. KGs is now
research-only; no further executable work is assigned there.

Luis asked whether practical tests should move to Shop first. The paper
recommendation is one controlled Shop learning cycle followed promptly by one
GE-1 scientific transfer. This remains a proposal, not an ownership transfer.
Planning inquiries were sent to Malleus Core Shop and KGs at Overlord's request.
They do not redirect approved atomic work or authorize implementation.

### Frozen evidence and private resume dependencies

Historical execution remains on Core
c95dba7b86bb61487bda9a52458e1ea47cce20ab, tree
39a9b9da7dcd41660b8b10fb20b1b1d8e2261082. Preserve the runtime export at
private/paper-v4-relationship-contrast-01/core and the earlier contrast inputs,
attempts, queries and reviews, especially b/attempts/attempt-03.

Preserve the complete ignored private/paper-v4-relationship-repair-01 directory:
producer/inputs contains the reading, ontology/imports, base capture, profile and
compiled surface; accepted contains ledger/history.jsonl, export-records.json,
retained-capture.json, replay-receipt.json, query and comparison; source-review
directories retain the assessments and their delivery records. These are not
recoverable from Git alone. Do not replace them with current shared source.

Preserve private/paper-v4-representation-design-01, including its manifest,
launch, input-delivery-verification and producer/work/representation-design.md.
The manifest references the repair packet's inputs; there is no separate
representation-design producer/inputs directory. All fourteen declared input
digests were checked again at this checkpoint and match. Design output remains
sha256:ffd5d233d98450d75dde895e2e061427ceded3e67c3be93a2fa31452612523ab.

The frozen accepted ledger is
sha256:3eeeac1c808ed918e501761c939bcb80cea94d10e3dc7aa5ef13b681d11e94db;
replay receipt is
sha256:5545fdb381e7de962a2eabfa7a57c19202f05c079ff4e57047d3207751014c32;
query result is
sha256:4a2ee8fb2c10c7ff4dfa4029df6b0b43956da3a5639f28306f14285d36fd9663.
These bytes were checked again, not regenerated. No fresh replay, semantic
assessment, full paper gate or improved result is claimed by this checkpoint.
The last E-0399 focused result remains 123 tests on its declared selector.

Preserve private/kg-representation-recon-2026-09-15, especially the GE-1
comparison, review-input map, B schema proposal and corrected YAML. These are
research proposals and constraints, not paper results. The optional separate
Core checker checkout /private/tmp/malleus-progressive-guidance.uxhila/repo
is an ephemeral dependency if that audit route is later selected. Its exact
coordinate is 25f94cbf0fe84a9a66b1511deb9b8a99b4c32709, tree
a9e4c42c8245e8db91cfb6dd842bc999fd9851df. Existence is not paper adoption;
verify availability again before relying on this temporary checkout.

### Minimum approval package and distinct acceptance criteria

Proposed owners: Shop supplies one controlled staged source case and executable
adopter tests; Core supplies only demonstrated generic missing capabilities;
KGs specifies meaning and transfer limits without coding; Paper owns independent
assessment and immediate scientific transfer. No generic EarthScience schema or
Shop business rule is promoted into Core merely for reuse.

1. Representation: freeze the case's meanings, alternatives and unknowns, then
   compile the selected schema and demonstrate legal populated examples plus
   tests that detect conflating the required roles. This establishes that the
   representation can express the case, not that a model will discover it.
2. Acquisition: separately approve a model condition and staged evidence
   delivery. Keep expected answers and previous outputs out of producer inputs.
   Independently assess each stage against the evidence available then, with
   exact witnesses, omissions and unsupported additions. Revisit the declared
   earlier interpretations, including completed ones. New observations alone
   do not establish that an earlier interpretation was reconsidered.
3. Accepted-state reconstruction: admit only the approved proposals through
   public APIs on isolated histories. Reopen from retained history and reproduce
   current records, old versions and evidence traces. Test refusal preserves
   accepted state and unaffected information remains accessible. This proves
   history mechanics, not source truth or semantic adequacy.
4. Readers: specify two independently implemented readers or interpretations,
   their common required meanings and abstention rules. Compare recoverable
   meaning and source attribution, not identical prose. Neither may infer a
   cause, timestamp, source resolution or support link from missing evidence.
   A guided repair is reported separately from fresh acquisition improvement.

For the first executable approval, select only the Shop case, its exact runtime,
local representation and controlled tests. A model run needs its own explicit
input/model/feedback/assessment approval after those checks. The PDF transfer
then requires a concrete B schema/runtime selection; no whole-PDF rerun or
mutation of the old accepted graph follows automatically. Select from-empty
demonstration versus historical amendment explicitly. The latter may require a
Core withdrawal/retyping capability if the chosen bibliography identity demands
it; that is not a prerequisite for an independent from-empty example.

Actual next step: collect Shop/KGs planning replies, reconcile a single bounded
proposal and return it to Luis through Overlord. Outstanding author decisions
are the Shop-first assignment, exact case/runtime/schema, model trial condition
and transfer boundary. Existing metadata/bibliography repair choices remain
open; do not claim that adopting this staged test plan resolves those records.

### Shop and KGs replies: two different candidate tests, neither selected

Shop's checkpoint was verified on disk at
/private/tmp/shop-combined-showcase.NMFkTZ/repo, codex/shop-combined-showcase,
HEAD 2fb66fc97c475f3cec0f2b2730062cdef24db59e, tree
fd7f73cf73972bfedd228d978b440f41b5796cce, src/malleus tree
f9439d9ef981c6f47ae894c32d171654b8fc3e4b. Only its existing
research/ontology_driven_kg_realization/experiments/small_shop/CONNECTED_STORY_PLAN.md
is uncommitted. Shop reports no approved atomic run remains active.

Shop proposes B's one-unit order, its two-unit update, then the retained prose
explaining why the earlier quantity was a mistake. This is useful for state
versioning, preservation and added explanation. It does not show an interpreter
correcting its own earlier meaning. Shop proposes one explicitly synthetic
misreading, treating the update as two additional units, to test correction.
No actual model or accepted result made that error. The control must never be
presented as observed extraction or admitted as a business fact. Its two current
read surfaces share one interpretation; warehouse JSONL/CSV equivalence supplies
no independent interpretation of B. No new Core capability is needed here.

KGs proposes a different contextual case: hold the historical table fixed and
later expose its retained shared-customer, policy and payment-delay prose.
Payment receipt and clearing are already in the table; the new prose can clarify
the source's explanation, while full account status remains unknown. This better
isolates context joining from adding transaction rows. It still does not ensure
a model will change its view, and justified no-change must count as valid review.
Both candidates need separately frozen exposure packets; the existing histories
already contain all context and cannot serve as blind stage-one inputs.

Paper recommendation to Overlord: distinguish the B packet as a small scripted
mechanics/control exercise from the fixed-table contextual packet as the closer
test of progressive interpretation. Do not approve both by implication or claim
that finishing B establishes semantic reconsideration. Luis selects the first
case and scope. No implementation or model run has started from this proposal.

KGs confirms no unresolved semantic choice blocks specifying GE-1 transfer
criteria. Preserve origin versus application, citation versus publication
resolution versus scientific support, reported uncertainty versus application
approximation, the direct caption statement versus our supported synthesis, and
the context actually available at each stage. Numeric equality proves neither
identity nor independence. Recover these distinctions from the graph with the
fixed readers, not by silently reinterpreting the original source in the reader.
Shop cannot establish geological applicability, uncertainty semantics, premise
support or inspection of a cited study. These remain scientific transfer work.
Detailed criteria are in KGs' owned
handover/2026-09-15-progressive-interpretation-overlord-handoff.md,
section GE-1 transfer acceptance criteria. Research-only status is unchanged.

### Reconciled recommendation: fixed table plus three context passages

At Overlord's explicit request, Paper inspected the actual table, context,
shipment_read_spec.json, shipment_explanation.py, shop.yaml and run.py. The
source hashes match those in both owner replies; the inspected reader is
byte-identical in shared main and Shop's pinned checkout. This is source/code
inspection, not execution of a new experiment or a model assessment.

Recommend the fixed-table/P1-context case as primary. Keep B and its synthetic
misreading only as reserve controls. The bounded source is the same 21-row table
at both stages, adding only intro-1-customer, intro-7-policy and intro-7-payment
at stage two. Do not add warehouse evidence, invoice balances, another account,
policy activation, new chronology or a shipment permission engine. The table
already contains payment receipt, clearing of both invoices and O2 shipment;
the added text supplies shared-customer scope and the author's policy/delay
explanation. This is a controlled information-exposure schedule, not new events
or a claim about when the retailer or original reader learned something.

Declare three interpretation subjects for review at both stages:

1. Recorded P1 receipt and its invoice-clearing connections. The table supports
   them initially. Later text may qualify the account without replacing those
   occurrences; justified no-change is a completed review, not a missed repair.
2. The reported relationship between the two orders and the explanation of O2's
   delay. Stage one lacks the selected prose context. Stage two permits testing
   its integration and attribution, without claiming independent causal proof.
3. Whether full-account evidence establishes shipment eligibility. The complete
   invoice inventory and explicit unpaid-status snapshot remain unavailable.
   This must remain specifically unresolved, not become a passing permission
   verdict because two clearing occurrences exist.

These are source-backed assessment obligations, not model answers, population
facts or a required outcome selected for a future model. Retain each actual
stage-one model output if a trial is later approved; stage-two dispositions must
address that output, not an evaluator-invented initial mistake. Review completion,
information gain, error correction and unresolved knowledge are separate results.

Two reader obligations: one recovers the bounded order/payment explanation,
including attribution and uncertainty; the other independently traverses its
represented objects, relations and evidence links to check the same distinctions.
Both must recover existing receipt/clearing facts, added context when represented,
and the persistent account limitation. They must not import missing meaning from
the answer key or silently rebuild it by rereading the source. Evidence remains
available to independent assessment for checking their claims.

Important current limitation: shipment_explanation._inputs reads all retained
context directly; shipment_read_spec.json selects passage roles, order scope,
the unpaid limit and account-completeness conclusion. run_story retains context
before any row import. The existing reader is therefore a full-context hybrid
reader, not evidence of graph-captured contextual meaning or blind stage-one
behavior. The local shop.yaml declares business objects, states and occurrences,
but no explicit customer-context/policy-assertion explanation representation.
Choosing the smallest local representation and checking its public compilation
is a prerequisite to the stronger graph transfer claim. No generic Core runtime
defect follows from this local modelling gap.

Raw context rows also include investigator-authored purpose/disposition fields.
Do not deliver those, the read specification or source_expectations.json as
natural source evidence to a model. Freeze exact source-text extracts and neutral
locator correspondence separately, retaining original source bytes unchanged.
Do not expose a full-context history or its derived graph at stage one.

Minimum proposed approval: this one source/exposure specification, local schema
proposal and scripted representability/preservation/reader checks. Exact runtime
and concrete schema require selection before executing; no runtime is selected
here. A subsequent separately approved model trial tests acquisition. Core is
needed only if that bounded public-path check yields a minimal reproducible
mechanism failure. Whole-graph policy work and bibliography retyping are not
dependencies. Independent semantic evaluation remains Paper-owned. Immediate
GE-1 transfer remains required after the Shop procedure is demonstrated.

### Owner agreement and stable Shop recovery

Shop subsequently confirms the reconciled P1-context recommendation, including
the local representation gap, separate exposure packets, independent reader
obligations and no demonstrated Core blocker. Both owners call it progressive
qualification/completion, not guaranteed self-correction. B remains reserve;
neither case is selected for execution. Stage-two passages are context rows
0, 2 and 3; neither authored purpose nor disposition is producer evidence.

Shop supplied stable recovery outside its temporary checkout at
/Users/luis/.codex/visualizations/2026/09/08/01a08375-3800-73a1-8eaa-915178c42f2c/shop-recovery/2026-09-15/.
Paper read its README and verified the three file digests, without restoring:
shop.bundle is 9e8760980e014975f907abee1bc58d82cc15e5f2d128f00efd1779d595d00f76;
CONNECTED_STORY_PLAN.md is 780a5ef36db4537896a79257bdc8312f7aefdbfb3ea79994bd2064c49e1baa60;
checkpoint.patch is 852ae21c5a4423ab03db5a4e09fc7fbd740fd63f78e995d47d7aa7c64e0ec1b5.
The incremental bundle requires base 30ab66437087f93b4f3e7b13b4d19229d2f7fca2
and preserves Shop tip 2fb66fc97c475f3cec0f2b2730062cdef24db59e. Shop reports
bundle verification; no full restore is claimed. Generated histories/explorer
are not copied into the bundle; the owned plan supplies reproduction commands.
This backup is not an approved source/runtime selection for the proposed case.

## Current resume point: E-0399, bounded review/repair specification

Read master 1.5.47 and the new E-0399 section of the existing
paper-v4/experiment-v4/RELATIONSHIP-REPAIR-RESULTS.md. Luis's Overlord relay
resumed preparation only. Three exact existing records are proposed for review:
the mixed first-page claim, the Yu article identity and the bibliography claim.
The specification binds their versions, evidence boundaries and preservation
obligations, with reader counterexamples. No review outcome or population is
authored. A review is not automatically a resolved issue or an admitted change.

Six new characterization cases are in test_relationship_repair.py. They bind
record versions, show metadata already in returned graph prose, resolve the
bibliography's actual capture, and demonstrate a synthetic date loss that the
existing exact-delta preservation check does not detect. That check protects
untouched records; it does not certify changed meaning. No semantic guard or
new accepted repair is claimed.

The optional Core checker is available at 25f94cbf0fe84a9a66b1511deb9b8a99b4c32709,
tree a9e4c42c8245e8db91cfb6dd842bc999fd9851df. A separate read-only audit invocation
is proposed, not adopted. Historical generation/replay remains on c95dba7.
Remaining choices: minimum metadata representation and bibliography identity,
then opt-in to that audit route. No new Core checker request is needed.

KGs owns GE-1; it confirmed no selected representation and supplied the required
source-local citation/publication/estimate-attribution/application interface.
Do not duplicate its comparison or repair its numeric/caption records. The
numeric subject says only crust; an RC2 substring in its ID is not typed location.

Validation: six new cases and the 123-test focused selector pass. Ruff/format,
scoped diff and source-copy checks pass. Seven frozen artifact digests still
match. This is a narrower selector than E-0398's 221 tests, not a full paper gate.

No experiment, reviewer, graph/schema amendment, new score, manuscript update,
Core modification, commit or ref movement occurred. Next free ledger entry is
E-0400, subject to checking other writers. Older sections are historical.

## Previous resume point: E-0398, one design submission complete

Read master 1.5.46 and the E-0398 section at the end of the existing
RELATIONSHIP-REPAIR-RESULTS.md. The one fresh Sol designer has finished. No
designer or reviewer needs polling or redispatch. Raw private output is
private/paper-v4-representation-design-01/producer/work/representation-design.md,
SHA-256 ffd5d233d98450d75dde895e2e061427ceded3e67c3be93a2fa31452612523ab.
It is a design proposal, not accepted ontology/population or an improved score.

Observed delivery is complete: fourteen exact inputs, 128 visible frames,
gpt-5.6-sol/ultra, fresh session 01a0a5a2-4091-71f1-a793-75f55120d5ba.
All ten source block locators and thirty-two current record IDs in its design
resolve. The source/capture chains behind all three cases and both preservation
controls match the supplied bytes. The stored current graph and replay receipt
reproduce exactly from the ledger. No input or accepted result changed.

The design separates A's reported occurrence/interpretation/metadata, B's graphic
conventions/boundary/estimate/application and C's publication/citation/attribution/
argument. Parent checked an additional real distinction: the body reports the
crust estimate for 8-Ma-old western-flank crust, while the caption applies it
beneath RC2. The typed observation has no observation_context and only a subject
named crust. Its original retained assertion still contains the missing scope.
The new regression fixes the diagnosis to the actual export, not an invented
copied-block claim ID; source blocks do not imply such graph records.

Do not accept the design wholesale. Preserve previously graph-accessible
publication metadata, not merely its source bytes. Define modality and function
before relabelling records; citation origin alone does not decide MEASURED versus
STATED. Do not transfer a schematic qualification silently onto a different
meaning unit. EvidenceRelation's description is narrower than its legal enum;
use the appropriate class for attribution.

The proposed Core need is conditional on C's chosen identity model. Replacing a
claim with a publication is unsupported by the frozen path, but the author must
first decide whether a meaningful citation assertion remains or the old identity
should be withdrawn. No dummy same-type successor, current-Core defect, Core
message or rebind is authorized. The optional date/citation/figure/function slots
are a menu of proposals, not an approved bundle. Choose the minimum correction
scope before generation. No second model or semantic retry occurred or remains
authorized by this pass.

Final focused selection: 221 passed in 35.51s; Ruff, formatting, scoped diff and
source-copy checks pass. Frozen reading, ontology, ledger, receipt, query, review
and comparison are unchanged. No full paper/Core/package gate, manuscript edit,
commit, push or shared-ref movement. Master stays 1.5.46. Next free entry E-0399,
subject to checking other writers. The older preparation snapshot below is
historical and does not authorize another launch.

## Previous resume snapshot: E-0397, one representation-design pass authorized

Read paper-v4/AGENTS.md and master 1.5.46. Luis approved the one fresh Sol
design-only pass proposed after E-0396. It examines the interrupted first-page
argument, mixed caption and bibliography. Its purpose is coherent meanings,
evidence/context, record reuse, category definitions and necessary schema/history
changes, not new population or grading.

Preparation is under private/paper-v4-representation-design-01. The designer
receives the full retained reading, accepted ontology/imports, current records,
their captures, historical guidance and one task. Questions, scores and previous
assessments are excluded. Definitions supplied as drafts may be challenged.
The only output is producer/work/representation-design.md. One fresh
gpt-5.6-sol/ultra submission, no inherited conversation, no additional model,
reviewer or semantic retry. Record actual execution and input delivery.

No Core/skill modification, rebind, runtime change, accepted-state write,
regrading, manuscript rewrite, commit, push or ref movement is authorized.
Frozen experiment Core remains c95dba7; the live shared main is not its runtime.
The known same-type/one-successor constraints are frozen capabilities, not an
established current-Core defect. No Core request follows automatically.

The E-0396 proposal and synthetic checks are in the existing repair report and
test_relationship_repair.py. The preceding focused selection passed 220 tests.
Read the September 14 handover for exact earlier run/repair evidence and limits.
No new design result is claimed by this preparation snapshot. Next free ledger
entry after this authorization is E-0398, subject to checking other writers.

Launch is observed: /root/sol_representation_design, session
01a0a5a2-4091-71f1-a793-75f55120d5ba. Initial turn_context confirms
gpt-5.6-sol/ultra and the session identifies this paper task as its parent.
The packet's launch.json retains the exact rollout path. All 128 frames across
fourteen inputs are now verified in actual model-visible outputs. The exact
verification is input-delivery-verification.json. This establishes delivery,
not understanding or source truth. Sol reports drafting the single submission.
The output remains pending; do not start a second designer or claim completion.
