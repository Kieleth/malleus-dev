# Overseer continuation handover, 2026-09-06 (written at Luis's request, 21:10Z)

Purpose: a fresh session continues the loop from this file, the journal (`handover/2026-09-05-overseer-journal.md`), the ledger (`paper-v4/paper-ledger.md`) and the memory (`~/.claude/projects/-Users-luis-Projects-malleus-dev/memory/`). Everything below is fact as of the timestamp; nothing is a plan Luis has not ruled.

## Where things stand

- Paper cells run-09 to run-21 are frozen and ratified (E-0205; human records in `paper-v4/evaluation-v4/run-NN/review-record.human.md`). Run-20 is the cell of record; run-21 its first replicate. Runs 17 and 18 (Haiku) refused before review.
- shop-01 (rows instead of prose, same skill, same Core c95dba7) is frozen (E-0203) and preliminarily reviewed (E-0204): P P P P, 2/5/10 of 17, because of an undeclared row-numbering convention and a review token stricter than the grammar. Not ratified; the v2 validator refuses row surfaces.
- Iteration 10 (v4.11) added `paper-v4/experiment-v4/type_set_closure.py`: a type set must list every surface subtype of every listed type; the next cell's binder calls it before writing the acceptance binding (shop-01's did).
- Luis's rulings of 2026-09-06 ~20:55Z are in E-0205 verbatim. Master plan items 9 to 12 in `paper-v4/paper-master-plan.md`.
- Latest paper commit on main at this writing: 66f56c5 (E-0205). Gate at that commit: 1592 passed (evaluation-v4 ignored while Paper-28 writes there).
- Next free ledger entry: E-0206. Only the overseer writes `paper-v4/paper-ledger.md`; agents never do.

## In flight (agents dispatched by this session; their reports arrive as task notifications; if the session is lost, their work is on disk, uncommitted unless they committed as instructed)

1. Question author (fresh Opus, reading only): wrote `paper-v4/experiment-v4/competency-questions-v3.json` (30 questions, DRAFT_FOR_FREEZE). Report received 21:08Z; to freeze (status FROZEN) and record as E-0206.
2. Core-21 (Opus): re-freeze `research/ontology_driven_kg_realization/experiments/small_shop/public_population/evidence.json` at the current Core, add its test dir to pyproject testpaths, write the skill's structured-source section with a compiler-validated worked plan, governance entry (next OVR after the head; Core-20 was OVR-000415), decision 26 in `design/KNOWLEDGE_PACKS.md`. Commits RED/GREEN, no push. One writer on Core's ledger: do not dispatch Core-22 until it reports.
3. Paper-28 (Opus): `paper-v4/evaluation-v4/review-protocol-v3.json`, v3 task template and blank record, `review.py` accepting v2 and v3 (declared evidence surface and locator convention; coverage per required semantic with derived responsiveness; assembly descriptor; controls; LOCATOR_NOT_RESOLVABLE → NOT_EVALUABLE by rule; support once per witness). Commits RED/GREEN, no push.

## Next steps in order (all ruled)

1. Freeze questions v3 (E-0206). 2. On Core-21's report: verify, push, then dispatch Core-22 (locator resolution at admission with a declared row convention, RED first) and after it Core-23 (a valid-time kind for "none stated"). 3. On Paper-28's report: verify, gate, push, ledger entry. 4. Harness v4.12 via a Paper agent: reachability (b) (subject-less records of listed subject-bearing types reached as ENTITY rows), canonical profile staging in prepare_producer, the closure check inside bind_from_surface; a new protocol version number; frozen cells untouched. 5. Replicates of the record condition (Opus 5, Core c95dba7, harness v4.10 producer block): run-22, run-23, run-24 (three to five in all counting run-21), each reviewed under v3 with the 30 questions. 6. Master plan item 12 (subject-less re-trigger) designed after v3. 7. shop-01 re-review under v3 with a declared convention is a ruling Luis has not given; do not do it unasked.

## The per-cell procedure (what every cell followed; commands as used)

Pin: `PYTHONPATH=$PWD:$PWD/src .venv/bin/python paper-v4/experiment-v4/run-NN/pin.py --commit <core commit>`; re-run must rewrite nothing (git status clean). Ledger entry, gate, commit, push.
Prepare: `paper-v4/experiment-v4/run-NN/prepare_producer.py --reading private/paper-v4-text-layer/selected-reading.json --output private/paper-v4-v4-run-NN/producer` (shop-01: `--output` only). Open `private/paper-v4-v4-run-NN/launch-log.json` (schema producer-launch-log/v2: schema, run, protocol, launches[{ordinal, role, phase, first_stage, launched_at, harness, requested_model, model_family, model_id, spawn_message, outcome, execution{pinned_commit, paper_commit, staged_at, interpreter}}], gate[], runner[], query{}, review{}).
Producer: Agent tool, model opus (record cells), subagent_type general-purpose, prompt = `run-NN/spawn-message.md` with `<PRODUCER_WORKSPACE>` replaced by the absolute workspace path. Fix and harness agents are Opus; never Fable.
Gate: `paper-v4/experiment-v4/run-NN/compile_ontology_candidate.py --ontology $P/work/ontology-attempt-0N.yaml --producer-root $P --output $R/gate/attempt-0N --attempt N` (the cell's own copy, never the root copy frozen at run-01). Citation check by WebFetch of every vocabulary_url and borrowed term; record fabricated, unverified, vocabulary_claims_unsupported.
Type sets: `$R/query-type-sets.json` + `.note.json`, written from the surface and the questions only; catalogue lesson (no catalogue type the question does not ask for; event type left out); closure: `paper-v4/experiment-v4/type_set_closure.py --surface ... --contract $R/gate/attempt-0N/validated-contract.json --type-sets ...` must print ACCEPTED; bind: `run-NN/bind_from_surface.py --surface ... --type-sets ... --replay-receipt PENDING --output $R/query-binding.acceptance.json`; stage `$P/accepted/{population-surface,diagnostic}.json`; record gate[] and query{} in the launch log.
Phase two: SendMessage to the producer agent id with the fixed text (run-21's in the transcript; shop-01's adds the contract identity from `shop-01/contract_identity.py`, the plan id prefix, the source ids, the profile identity = digest of the profile's canonical bytes, the grammar string and canonical-bytes rule). Record `launches[0].phase_two` with `dispatched_at` read from the session transcript jsonl (never the bash clock).
Runner: `$S/runNN_exec.sh "$T"` (scratchpad; T written to `$R/transaction-time.txt`; results/ and ledger/ must not pre-exist). On refusal: `mv $R/ledger $R/refused-runner-attempt-0N/ledger`, capture the exact diagnostic, record runner[] with classification (STRUCTURAL_REFUSAL_RETURNED_TO_PRODUCER counts; parent omissions recorded as HARNESS_INTERFACE_OMISSION_RETURNED_WITH_COORDINATES / PARENT_INTERFACE_ERROR_RETURNED_WITH_COORDINATE and `counts_against_diagnostic_budget: false`), return to the producer with the diagnostic verbatim; two structural returns, a third refusal is terminal.
After admission: re-bind with `--replay-receipt <run-result.replay_receipt_sha256>` to `$R/results/native-query-binding.json` (cases_sha256 must equal the acceptance binding's); `run-NN/native_query.py --ledger $R/ledger/history.jsonl --binding $R/results/native-query-binding.json --results $R/query`; record runner[] (admitted) and query{} (rows keyed NQ-CQ-0N) and launches[0] usage (usage_cumulative = phase one; usage_by_resume cumulative); `run-NN/usage_from_launch_log.py --launch-log ... --output $R/usage.json` (refuses overwrite and undeclared top-level keys).
Freeze: scratchpad `freeze_runNN.py $S/ladder.py $S/e0NNN_body.txt` derived by sed from the previous (fix the E-number guard; the template has accumulated fixes: load `log` first, urls from the citation check, dynamic event types and model fields, `diagnostic_returns: len(log["gate"])-1`, phase_two block required with dispatched_at > bound_at). For a non-standard cell (shop-01) a Paper agent froze it from a brief. Ledger body: no count enters it before the script that shows it (Paper-27 caught three of mine).
Gate: `ARGS=$(.venv/bin/python -c "import json; m=json.load(open('paper-v4/active-test-manifest.json')); print(' '.join(m['pytest_args']+m['paths']))"); eval "PYTHONPATH=$PWD:$PWD/src .venv/bin/python -m pytest $ARGS" 2>&1 | tail -1 > $S/gate.txt`; commit only after `grep -q " passed" gate.txt && ! grep -q "failed\|error" gate.txt`; the gate runs AFTER every ledger append (E-0202 was committed red because it ran before). Use `--ignore=<dir>` for an agent's in-flight directory and say so in the commit.
Review: two fresh Opus sessions (Agent, general-purpose), CQ-01+02 and CQ-03+04, prompt = run-21's review dispatch text in the transcript with the counts moved; merge with `$S/merge_runNN_review.py` (set expected counts), `review.validate_review`; record review{} in the private launch log (dispatch times from the transcript); ledger entry; RCA `handover/2026-09-0N-<cell>-rca.md`; journal; memory; commit explicit paths; push.
Commits: explicit paths only, never `git add -A`; never revert/reset/stash/checkout/amend; trailers `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>` and `Claude-Session: https://claude.ai/code/session_01MPL2T15oYt4gU8DapAcSVV` (agents: Claude Opus 5). Public repo: fleet project names never in public docs; nothing sharing a 60-character run with the reading leaves private/ (ladder). Never commit the PDF.

## Scratchpad inventory (`/private/tmp/claude-501/-Users-luis-Projects-malleus-dev/264ec7c2-cb80-475d-b4b7-84db254b9077/scratchpad/`; lost with the machine's tmp, so nothing irreplaceable lives there)

`freeze_run21.py` (one-attempt freeze template), `merge_run21_review.py`, `merge_shop01_review.py` (no validator; its locator regex flags file-first locators wrongly), `run21_exec.sh`, `shop01_exec.sh`, `ladder.py`, `reliability.py` (also committed at `paper-v4/evaluation-v4/reliability.py`), `rca21.py` (variance table run-20 vs run-21 from frozen files), `unlisted_rows.py` (committed at `paper-v4/experiment-v4/unlisted_rows.py`), `e0196_body.txt`, `e0203_body.txt`, `shop01-rca-draft.md`, `ratify/` (the eleven digests). The exact phase-two and review dispatch texts are in the session transcript jsonl (`~/.claude/projects/-Users-luis-Projects-malleus-dev/264ec7c2-cb80-475d-b4b7-84db254b9077.jsonl`, SendMessage and Agent tool_use inputs).

## Rules earned in this loop (also in memory)

Check the output, never steer the producer. No count enters a record before the script that shows it. Read the mechanism's own constraint before claiming what it does (the executor's own-type projection; the runner's canonical-bytes rule; the profile identity). Dispatch times from the transcript. One writer at a time on Core's ledger; the overseer writes the paper ledger. Decisions go to Luis in chat, in full; files are the archive. Report bad news first; corrections in place with their cause.
