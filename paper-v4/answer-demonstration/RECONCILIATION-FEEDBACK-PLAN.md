# One correction after source review

E-0292, master 1.5.24. Luis approves the proposed correction after the comparison
of the two withheld proposals. This is one explicitly disclosed semantic-feedback
condition, not a new question-blind sample or a retroactive structural return.

Use the same Sol/ultra producer session from sol-reconciliation-01. Preserve that
run, its rejection and all input/output bytes. Freeze a sibling
sol-reconciliation-feedback-01 over the same accepted sol-qualification-01 history
and depth-method-01 reader. Core remains 160878cf14c0d27b11a440e26688708e9b7a7e2b.
No source, reading, ontology, number/unit, question or query change.

## Sequence

1. Test distinct condition/artifact identities, frozen feedback/input lineage,
   exclusion of old judgments from fresh review and the existing pre-retention
   authorization guard. Reuse the current reconciliation checks and public runner.
2. Deliver the prior candidate, report and complete source review to the same
   producer, with the unchanged source, accepted graph, ontology and captures.
   Verify exact new feedback delivery and continued model/effort. It writes one
   corrected complete proposal and report, or an explicit incomplete/refused result.
   At most two exact structural returns; no further semantic-feedback round.
3. A fresh independent reviewer assesses every changed property, relation and
   report context against the source and actual baseline. It does not see the
   previous review or the correction feedback. Reuse the same support rule:
   one unsupported or unresolved asserted change withholds the whole batch.
4. Only a supported batch can pass the paper-owned exact-review authorization
   guard, then use public admission on a sibling ledger. Verify unchanged records,
   exact version/relation closure and old history prefix; reproduce at one explicit
   transaction time. Run the thirty unchanged questions without source access.
5. Independently assess every changed answer before and after under the existing
   required semantics. Preserve earlier grades, report gains and losses, and do
   not infer a new full-suite score. Human ratification stays pending.

The six Observation targets, permitted contextual edits/removals, full-block new
evidence, capture-scoped references, exact actual-value references, protected
numbers/units and incident-relation replacement rules are unchanged. The model
must address the review's reasoning and evidence concerns, not merely remove a
flagged enum to obtain admission. The coordinator does not write replacement facts
or select a passing subset. Previously supported work should be preserved unless
source-grounded reconsideration is explicitly explained.

No new entities, independent relations, ontology revision, general semantic
validator or automatic source scorer. Core and manuscript remain untouched.
No dependency, automation or shared Git mutation. If this correction remains
unsupported, record the result and the next concrete cause without another sample.
