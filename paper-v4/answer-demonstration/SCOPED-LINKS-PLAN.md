# Source-grounded scoped links

E-0266. Luis approves the proposed amendment. Master 1.5.19.

1. Freeze the accepted sol-argument-01 graph, its 26-event ledger and three
   capture artifacts. Use the already corrected scope-method-01 query code and
   its reproduced query result as the before condition. Keep Core 160878c,
   reading, ontology, thirty question programs and argument-criteria.json fixed.
2. Add TDD guards for the successor's fresh namespace, current baseline,
   corrected reader and complete retained evidence. Exact duplicate relation
   payloads with different IDs must refuse; qualifiers are not guessed away.
   Reuse the existing staging and admission code, no second runtime pipeline.
3. Dispatch one fresh Sol/ultra producer with complete verified input delivery.
   It sees the declared question and unchanged criteria, but no expected values,
   selected evidence endpoints, query code, prior review or rationales. Only new
   ResearchRelation links involving the existing hypothesis and existing entity
   endpoints are allowed. Preserve every accepted record. No quotas or semantic
   feedback. At most two exact structural returns; retain every attempt/refusal.
4. Admit through the existing public Core path on a separate ledger copy.
   Mechanically preserve the prefix, old records/history and unchanged queries.
   Repeat the admitted execution byte for byte. No source access during queries.
5. Independently review every added relation and changed-answer witness, before
   and after under the same prospective criteria. Record gained information,
   remaining qualifications and scope, not only a coverage label. Preserve old
   reviews. Human ratification stays pending; no global rescore or success quota.

The current task is evidence-link selection after a separate projection fix.
It cannot repair missing qualifiers in existing records because record changes
are not authorized. A refusal or partial answer is retained as the actual result.
No further producer sampling, Core request without a reproducer, manuscript edit,
dependency change or Git mutation.
