# One fixed-ontology Sol capture

Approved by Luis, 2026-09-06. Local case name: followup-sol-01, under the
answer-demonstration directory. This does not allocate or resume Fable's run-22.
The additional Cerebras/NIST cases are reserved for later.

## Fixed and changed inputs

Retain the selected reading and the accepted run-20 ontology with its exact
six-source definition closure. Compile those unchanged bytes against the final
Core requirement cut. If compatibility requires a domain ontology change,
report the refusal and return that decision to Luis; do not repair the ontology.

Use one fresh gpt-5.6-sol producer with no inherited context. Its declared inputs
are the selected reading, fixed ontology/import definitions, the identified
generic Core skill, canonical source-assertion profile, compiled population
surface and interface coordinates. It sees no competency questions, queries,
old populations, results, reviews or manuscript. It has no network or delegation.
This is a declared session boundary over shared storage, not an OS sandbox.

Unlike the original two-phase cells, this session performs population only from
an already selected ontology. Model, guidance and prior ontology-acquisition
context differ from the original Opus condition. This is an exploratory adopter
comparison, not a replicate, a model ranking or an isolated causal test of the
guidance changes. Opus can be considered later.

## Before launch

1. Receive and inspect the exact final Core coordinate, not its moving checkout.
2. Check unchanged ontology closure and reading identities. Compile publicly;
   retain the resulting surface and history-profile identity.
3. Freeze the corrected query source and binding before any new population is
   visible. Pilot-04 query source is sha256:1ace62715e813125fce9ec5b38278faae25018468069b22c52bf2d87ea1bbdb1.
4. Stage a new private producer directory with an exact input list. Record
   actual dispatch time, requested model and fresh-session identity. The
   default reasoning setting is not an observed or independently pinned value.

## Execution and terminal outcomes

The producer writes one document-population file under the shipped capture
grammar, plus its session/status record. It reviews every block or explicitly
accounts for unavailable material. It may record gaps; it may not revise the
fixed ontology or create question-specific facts. Return exact typed structural
diagnostics at most twice to the same session. Preserve every submitted file
and refusal. A third structural refusal is terminal. No hand repair or fallback.

Use the public document adapter, population preparation and structural admission
path. Core owns generic mechanics, never this experiment's inputs or decisions.
Reopen from disk, compare replayed state and receipt, then query under the
existing source-free guard and trace the witnesses. Treat NO_DOMAIN_CHANGE as
a recorded terminal outcome rather than pretending a change was admitted.

Review all thirty question outcomes against exact sources and complete accepted
definitions, with explicit model authorship. Keep missing relationships,
qualifications, unsupported fields and ambiguous candidates visible. Do not
require a SUPPORTS edge or a score threshold to accept the experiment's result.

The manuscript distinguishes the original reviewed captures, query-only
correction on those graphs and this fresh population condition. No missing
edge is added by the query or evaluator. No result is silently substituted.

## Coordination

Only generic census, claim/relationship guidance and Small Shop conformance
requirements are sent to Core. All capture orchestration, producer choice,
query binding, evaluation and manuscript work stays here. Core's independent
Sol Shop run is neither a paper run nor a substitute for this one.

## Frozen dispatch

Core commit 160878cf14c0d27b11a440e26688708e9b7a7e2b, tree
d946a7550dee247a371889f5eadf1cfecc964450. This is a local committed cut, not a
published release. Its six capture-coverage conformance tests were rerun here
and passed. Core reports 117 passed and 1 skipped in its scoped gate, with
three independently reproduced pre-existing Shop receipt mismatches excluded
from any green-suite claim.

The unchanged run-20 ontology closure compiled successfully. The exact producer
packet is private/paper-v4-answer-demonstration/followup-sol-01. Manifest digest:
sha256:bd22c14b6b47e1cad22cd9603cb5182586f4f117ed9d3cac167971374117666c.
Frozen task digest:
sha256:27b67ee46d47c624f37778226b48679927158883d2c6e43a29dd3494e72e8180.
The requested model is gpt-5.6-sol; fresh agent /root/sol_fixed_capture was
dispatched between 2026-09-06T23:51:37Z and 23:51:54Z. The tool did not expose
an exact start timestamp or independently measured reasoning setting. The
manifest retains its original staging status; this entry records dispatch.

Pre-dispatch checks: 166 paper tests passed, Ruff and formatting passed. A third
software-only staging check at this Core coordinate admitted no domain change,
reopened nine retention events, and ran thirty empty queries with zero forbidden
access attempts. The earlier empty harness check exposed a misleading query
flag, replay_matches_admission, on NO_DOMAIN_CHANGE. Two regression cases now
require replay_matches_preclose and an explicit admission_occurred boolean.
The corrected precheck reports false for admission. Earlier private precheck
bytes remain intact; none is a producer attempt or document-coverage result.
