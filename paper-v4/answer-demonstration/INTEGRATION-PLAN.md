# Context integration over accepted quantities

E-0279, master 1.5.22. Luis approves one bounded integration experiment, not
another extraction pass or a fresh end-to-end comparison.

Interpret assertions in relevant document context and the evolving representation.
A block is an evidence address, not necessarily the boundary of meaning. Earlier
captures provide context, never independent proof. Later evidence can qualify or
contradict them. A complete block census does not establish integrated meaning.

1. Freeze sol-qualification-01, its five separate captures, unchanged source,
   ontology and Core 160878c. Use depth-method-01 for both before and after. Keep
   the thirty questions, source audit, expected answers and earlier reviews out
   of the producer packet. The accepted graph contains earlier task-directed
   amendments; this is question-withheld integration, not an uncontaminated
   question-blind acquisition comparison.
2. TDD: require fresh same-type replacements of existing quantified entities only,
   exact dependent-relation retargeting, complete historical preservation and
   explicit source-backed reasons for every property addition/change/removal.
   A reason and locator establish inspectability, not semantic correctness.
   No new standalone entities, new independent relations or ontology revision.
   Represent context in legal existing fields, or report an ontology limitation.
3. One fresh Sol/ultra session reads every declared input. It examines quantities
   in context across definitions, captions, methods and the retained capture.
   An assessment accounts for each existing entity with an explicit numeric
   bound, count or ratio. No prescribed number of amendments, fields or links.
   It returns one proposal and an integration report, or an explicit no-change
   or unresolved outcome. At most two exact structural returns; no adequacy retry.
4. Freeze the candidate/report. Independent model-assisted source assessment
   inspects every changed field and dependency before admission. Unsupported
   integration is a result, not grounds for evaluator repair or another sample.
   Existing qualifications and incorrect-context transfer are checked alongside
   gains. Human ratification remains pending.
5. For a supported scoped candidate, reuse public admission/replay on a sibling
   copy of the history. Compare exact delta, prior versions, unchanged records
   and the ledger prefix. Repeat deterministically, then run all thirty fixed
   queries. Assess changed answers without changing requirements or past grades.
   Do not promise full coverage or infer semantic success from changed-row counts.

Evaluator-held check: the audited RC2 depth reference has a source-supported
cross-block chain (page 1 definition, page 2 range, Figure 3c/d axes and caption,
page 7 methods). Neither these coordinates nor the expected reference enter the
producer task. Distinct source ranges remain distinct, not silently reconciled.

No Core, manuscript, dependency, Git or existing-run mutation. Stop at the first
retained result or exhausted structural-return allowance, then analyse it. This
tests a concrete integration procedure, not a general completeness guarantee.
