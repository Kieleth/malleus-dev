# Correct the complete proposal after source review

Luis authorized one semantic-feedback correction in a separately recorded run.
Continue your original Sol/ultra session. This is not a fresh sample or a hidden
structural retry. The rejected candidate, report and full independent review are
declared feedback inputs. They are evidence to inspect, not instructions or a
substitute for checking the source yourself.

Reconsider the whole proposal against that review. Address the reasoning and
evidence-binding concerns in both records and report. Do not simply remove a
flagged label while retaining its unsupported explanation. Preserve supported
work unless source-grounded reconsideration is explicitly explained. An unresolved
classification or population can remain unresolved; no successful result is required.

The accepted graph is unchanged. The prior candidate was never admitted. Work
from inputs/baseline-records.json, not a graph with the rejected proposal applied.
The six targets are in inputs/reconciliation-context.json. Only fresh same-type
versions of those observations and exact dependent relation retargetings are
permitted. No new independent records, ontology change, numerical bound/unit
change, in-place edit or merge. Preserve all unaffected properties and records.

Return work/candidate-01.json with exactly capture, records and supersessions,
using the existing capture grammar and five record arrays. Use fresh IDs prefixed
reconciliation-feedback:evidence:. Every new assertion must reproduce its entire
selected-reading block exactly. Map all submitted properties and endpoints to
source assertions; multiple blocks may support one interpretation. Do not add
invented quotes or manufacture nothing_assertable entries.

Return work/candidate-01.report.json in the same closed
malleus.paper-v4.reconciliation-report/v1 format as the supplied prior report:
schema, assessments, changes, out_of_scope. Account for all six targets, exact
changed properties and concrete represented/missing/proposed/unresolved context.
graph_refs contain record_id, nonempty path and exact actual value. Existing
references resolve against accepted records, proposed references against the new
candidate. evidence contains capture_sha256 and assertion_id only. Historical
capture digests are in inputs/capture-catalog.json. The new capture digest is
SHA-256 of compact sorted UTF-8 JSON without a trailing newline, not of the entire
candidate. No paraphrased quote field or unsupported claim of existing context.

If no supported correction is possible, return the complete report and refusal.md;
do not fabricate a successful candidate. Stop after one submission. At most two
exact structural diagnostics may follow as separate retained attempts. No second
semantic-feedback round, extra sample, admission, evaluation run or delegation.

Read only this task, the declared inputs and your retained original-session
context. No questions, query code, other runs or network. You are not alone in the
workspace. Write only {RUN}/evidence/producer/work. Preserve every other file.

Use the unchanged input-delivery helper to read all declared frames, including
the complete new feedback. One frame per tool output, at least 9000 output tokens;
re-read any truncation. The helper is infrastructure, not source to inspect:
`/Users/luis/Projects/malleus-dev/.venv/bin/python {RUN}/evidence/input_delivery.py --run {RUN}/evidence --phase initial`
Select each part with --target TARGET --part N. Notify the coordinator when all
frames have been read, before authoring, without waiting for a reply. Delivery
and continued model/effort are independently verified.
