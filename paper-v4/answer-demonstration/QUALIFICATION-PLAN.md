# Qualified evidence without changing the observation

E-0272, master 1.5.20. Luis approves separating represented-claim faithfulness
from task sufficiency, retaining identified meaning-changing omissions as defects.

1. Freeze the accepted sol-argument-scope-01 history, all four capture artifacts,
   unchanged source/ontology/Core 160878c and corrected thirty-query reader.
   Freeze qualification-criteria.json and the review task before dispatch.
   Seismic coverage requires evidential status, axial/site scope and the source's
   observation-window limitation. No answer values or expected enum are supplied.
2. Write failing tests, then guard one atomic same-type observation replacement
   and its single incident relation replacement. Existing values and all other
   state stay fixed. Only determination, assertion_modality, description and
   duration may be added to the observation. These existing ontology fields can
   express the requested qualifications; no particular value is prescribed.
   Retarget the same relation to the new observation, preserving everything else.
   Refuse unexpected dependencies or expanded scope rather than silently repairing
   them. Semantic truth remains an independent source-grounded judgment.
3. One fresh Sol/ultra producer reads the complete source, ontology, current graph,
   four captures, question and prospective criteria with verified frame delivery.
   It authors the two-record candidate or refuses. It receives the target IDs and
   bounded finding, no old reviews, rationale, query code, replacement content or
   expected enum. At most two exact structural diagnostics, no semantic coaching.
4. Reuse the existing admission executor on a copy of the ledger. Verify the old
   prefix, history, all unrelated records, exact supersession and graph closure.
   Replay and repeat execution byte for byte. Run all thirty unchanged queries;
   explain any changed objects rather than require a predetermined answer count.
5. Fresh independent model-assisted assessment applies the same prospective rule
   to before/after. Judge asserted properties and paths separately from missing
   coverage. Require a named unsupported claim or implication for faithfulness
   failure. Check both replacements, every changed-answer witness, all capture
   namespaces and the five declared semantics. Preserve old reviews. Report field
   gains, unresolved assumptions and limitations; human ratification stays pending.

This is finding-guided qualification repair, not question-blind acquisition or a
causal model comparison. No full-coverage guarantee, extra sampling, Core change,
manuscript rewrite, automatic semantic grader, dependency or Git mutation.
