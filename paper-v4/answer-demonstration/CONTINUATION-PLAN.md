# Query binding and one capture continuation

E-0247, 2026-09-07. Luis approved both improvements proposed after E-0246.
Master 1.5.13. Preserve sol-e2e-corrected-01 and every historical artifact.

## Fixed boundary

Core remains 160878cf14c0d27b11a440e26688708e9b7a7e2b, tree
d946a7550dee247a371889f5eadf1cfecc964450. The source reading, accepted ontology,
compiled surface, skill and thirty questions remain fixed. Same existing Sol
producer, same low effort. No new producer sample, ontology revision or hand
authored fact. No Core change, network ingestion, dependency install, commit,
push, shared ref movement, heartbeat restart or manuscript edit.

## Milestones

1. Fix query candidate-text binding. Classify the accepted surface's string
   slots explicitly as searchable content or excluded metadata. Bind names and
   types only, never record IDs or answer values. Refuse unknown string slots,
   invalid declarations and undeclared configuration. Identifiers, locators,
   digests, foreign-key values and enum labels do not become search prose.
   Keep the thirty question programs, numeric tests, projections and actual
   relation traversal unchanged. No inferred relationship or source lookup.
   This corrects the demonstrated text-slot defect, not every historical
   question's vocabulary assumptions. Record remaining unsupported shapes.
2. Freeze and execute the new binding against the original graph. Reproduce
   its original queries first. Check ledger and graph immutability. Review the
   new answer view independently. This retrospective query-only result must
   not replace the original review or count as capture improvement.
3. Freeze the same query method before continuation. Give the original producer
   only its own declared inputs, its own accepted outputs, the compiler census
   and a generic whole-source capture worklist. No question, query, score,
   reviewer judgment, expected answer, record quota or requested edge. Ask for
   one whole-source pass and a complete consolidated revised population, with
   faithful qualifications and explicit remaining gaps. Preserve original files.
   Record the worklist and final limitations. At most two exact structural
   diagnostic returns, with no second semantic feedback round.
4. Run the revised capture through the existing public from-empty runner in a
   separate history, then reopen and reproduce exactly. This tests capture
   continuation, not a ledger migration, delta or semantic re-entry. Compare
   the old graph and revised graph using the same frozen new query binding.
   If the model refuses or structural retries exhaust, report that outcome.
5. Complete source-grounded model-assisted review of all thirty questions and
   required semantics, with human ratification pending. Distinguish capture
   additions, lost or changed records, query reachability, source fidelity and
   answer coverage. More rows alone do not demonstrate improved knowledge.

## Mechanical guards before code

Paper-local changes only. No server interaction or endpoint. Required inputs
fail loudly. Historical implementations remain immutable experimental controls,
never a fallback for the new condition. Add RED tests for the new slot missed
by the old filter, unknown-slot refusal, metadata exclusion, exact projection,
missing numbers, non-inferred edges, and producer-packet question exclusion.
Keep source-bearing artifacts private. Do not weaken Core admission or convert
evaluation labels. Reuse the existing read guard, public runner and frozen
review protocol. Record changes and errors in the ledger, not the manuscript.
