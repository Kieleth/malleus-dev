# Complete one evidence argument

E-0259. Luis approves the proposed completion pass. Master 1.5.17.

Start from sol-links-01/evidence/attempt-01: 138 entities, 21 relations and one
domain event. Preserve its twenty-event history and every accepted record.
Keep Core 160878cf14c0d27b11a440e26688708e9b7a7e2b, its exact source/ontology
closure and the frozen SubjectGraphReads/thirty-query programs unchanged.

1. Freeze CQ-T5-01 and explicit task-specific meanings of its five required
   semantics. The same meanings are supplied to producer and reviewer. They
   distinguish evidence records from merely restating a hypothesis, concentration
   evidence from naming a gas, and earthquake depth from modelled saturation
   depth. Each asserted evidence role needs an explicit source-supported path.
   These definitions are new prospective criteria, not retroactive adjudication
   of earlier reviewers' disagreements. No expected answer values or endpoints.
2. Freeze one fresh Sol/ultra producer packet with the source, complete ontology
   closure, current graph and both retained captures. Verify every input frame
   before authoring. No query implementation, earlier scores or rationale.
3. Allow only new ResearchRelation additions involving claim:co2-degassing and
   existing entities. No mandatory edge count, rewriting, new entities/values,
   supersession or ontology change. Use fresh record/assertion and history
   artifact identities. A faithful refusal or incomplete result is acceptable.
   Keep each candidate; return at most two exact structural diagnostics.
4. Reuse the existing public admission/replay runner on a separate history copy.
   Require exact record/history preservation and an unchanged ledger prefix.
   Run all thirty frozen query programs and reproduce the execution exactly.
5. Independently assess every added relation and changed-answer witness against
   full source context. Assess before and after using the frozen completion
   definitions and existing v3 support/coverage labels. Do not import missing
   values from source into an answer or convert modelled results to measurements.
   Keep qualifications and unresolved requirements visible. Human ratification
   remains pending. Do not replace earlier grades or report a new full-suite score.

The question and explicit use requirements are now intentionally visible. This
tests a bounded task-directed improvement to accepted knowledge, not autonomous
whole-document completeness, why the earlier model stopped, or semantic re-entry.
No Core request without a concrete capability failure. No further sampling,
manuscript edit, dependency install, commit, push or ref movement.
