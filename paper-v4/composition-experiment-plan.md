# One task-directed composition amendment

Version: 0.1.0. Author approval: 2026-09-06, "go".

This is a separate task-directed repair of Capture B. It does not replace the
question-withheld captures or the earlier finding-guided repair. Start from a
new copy of original B, not the previous amended history, so both repair
conditions share the same starting state. Preserve all prior runs and results.

## Fixed boundary

Core c95dba7b86bb61487bda9a52458e1ea47cce20ab, original selected reading,
ontology/import closure and pilot-03/run-21 query programs remain fixed.
Reproduce the original receipt, exported graph and thirty query outputs first.
No ontology rebind, new source, measurements, entity edits or query correction.

The producer is a fresh Sol session with no inherited conversation. It receives
the reading, accepted records, capture, ontology closure, existing capture
grammar guidance and the exact CQ-T5-01 question with its five requirements.
It receives no expected answer values, query code, previous reviews, repair
candidates or successful-output examples. This is a declared file-access boundary,
not an OS sandbox. The evaluator authors no population facts.

Only new ResearchRelation records between existing entities and the existing
degassing hypothesis are permitted. No supersession. The producer must decide
which connections the source supports, preserve epistemic qualifications, and
refuse or report insufficiency if the fixed representation cannot express the
requested answer. No relation is mandatory merely to improve coverage.

## Sequence and terminal outcomes

1. Freeze the question, inputs, task and this plan before dispatch.
2. One fresh producer proposes a delta or a reasoned refusal. Return at most
   two structural diagnostics to that same session. Retain every candidate.
   No semantic-review feedback loop, evaluator repair or fallback.
3. Admit through the existing public adapter, population compiler, preparation
   and structural history helper on a ledger copy. Reopen and replay. Require
   byte-preserved base prefix, unchanged existing records/history and exact
   candidate-to-projection agreement.
4. Execute all thirty unchanged queries. Report every changed answer and
   unchanged output; do not assume only the target question changes.
5. A separate reviewer inspects every added relation, its endpoint meaning and
   trace, and each changed answer. Assess every required semantic against the
   exact source. No exact-match scorer. Distinguish modelled from observed
   quantities and source-attributed hypotheses from established causes.
6. Reproduce successful admission/replay/query output using retained candidate
   bytes and explicit protocol time. Report structural and semantic results
   separately. Human ratification stays pending.

A supported complete answer, a supported partial answer, a semantic refusal or
a structural failure is a valid terminal result. Do not run until a favorable
score appears. No new aggregate primary score, model comparison, general
semantic-repair guarantee or Core-owned evaluation. Send Core a request only
for a demonstrated missing generic capability with a minimal reproducer.

## Publication integration

Add the outcome as a separate task-directed condition, with source and record
exhibits in the appendix. Preserve primary counts and the first two amendment
results. No commit, push, tag, submission or shared Core mutation is authorized
by this experiment. Publication integration and review ratification remain
separate author decisions.
