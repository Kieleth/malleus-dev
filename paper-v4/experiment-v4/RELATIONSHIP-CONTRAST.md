# Does contextual relationship guidance improve a fresh capture?

2026-09-14, E-0376. Sol selected for both conditions. The current handover and
launch records carry execution status.

Current selection: gpt-5.6-sol/ultra, fork_turns=none, one fresh session per
condition. Ultra retains the setting recorded for sol-fresh-comparison-01.
Both new tasks use relationship-producer-task.md and the existing exact-frame
delivery helper. That administrative delivery change is identical across the
pair; no exact match to the older Opus launch environment is claimed.
prepare_pair now stages fresh private workspaces and refuses overwrites or
destinations outside private/. Eleven contrast/staging tests pass, eighteen
with the existing next-run tests. The helper still invokes no model.

Historical comparison: sol-fresh-comparison-01 is the successful fresh Sol/ultra
reference, with 126 entities, 76 relations and two events. Its different Core
pin and reconciliation checklist prevent single-factor attribution against the
new runs. Its answer totals also use an older reader/review instrument. Keep
them unchanged; any common-reader reassessment must be separately identified.
Fixed-ontology captures and guided amendments are separate capability controls,
not fresh-acquisition baselines.

The prior capture kept the evidence and the hypotheses but left some arguments
disconnected. Its instructions discouraged deriving a relationship from nearby
sentences. Core has already corrected that instruction. This experiment tests
whether consuming the correction helps, rather than requesting another Core fix.

## One difference

Both conditions use the eight inputs recorded for run-26. The old condition
uses those bytes unchanged. The contextual condition replaces only the three
lines beginning `A relation's endpoints` with Core's paragraph beginning
`Distinguish a proposition identity`, ending before `Inspect the returned`.
That paragraph permits source-supported relationships whose endpoint names do
not occur in one sentence, preserves attribution, and warns that co-occurrence
alone supports no relationship. This is one instruction-block treatment, not
an experiment isolating each sentence within the replacement.

Core's correction also changed coverage-accounting guidance. That separate
change is excluded. Subject naming, census instructions, runtime, document,
packs and history profile remain unchanged. The contextual skill is therefore
an explicitly derived experimental input, not the complete skill at Core's
correction commit or a test of current adoption.

`relationship_contrast.py` reconstructs both inventories in memory and refuses
any difference outside the selected paragraph. It checks the original manifest,
source bytes and staged bytes against their identities. Its prepare_pair entry
point stages checked inputs, a common task and the delivery helper in fresh
private workspaces. It neither invokes models nor changes an installed Core
skill or a historical preparation script. Its nine input tests cover undeclared/missing
inputs, empty data, changes to the reading or other instructions, drift shared
by both conditions, and missing or repeated replacement boundaries. These tests
prove input control, not a model effect.

## Execution

Use two fresh producers, each constructing its own ontology and then populating
it in the same session. Supply neither questions, prior ontologies, prior
populations, the RCA nor this experiment note. The old completed run is context
for diagnosis, not a substitute for the new control. No evaluator-authored facts,
hidden retries, edge quotas or article-specific relationship hints.

Both launches need the same exact model and observed execution settings,
identical task wording apart from workspace/interface identifiers, the same
feedback limits, and retained launcher metadata. Historical Opus effort was
not observed, so do not claim an exact setting match to run-26. Both new Sol
launches explicitly set ultra and actual rollout contexts confirm it. Initial
delivery is verified against all 34 exact returned frames for each producer.

Before dispatch, freeze the common task, model/effort, compiler-feedback limits
and actual staged input identities. Use the existing diagnostic limits, two
compiler returns and two source-located additive revision rounds, unless the
author changes them. A failed or partial capture stays a result. Two single
captures are a diagnostic pilot, not evidence of a reliable improvement rate.

## What to inspect after capture

Keep these checks outside the producer's inputs. For each scientific argument,
locate the retained source statement, the records expressing its premises and
conclusion, and any explicit link between them. Check direction, attribution
and qualification against the source. A status field or source trace without a
graph link is reported separately, not silently counted as the same result.

Use the depth-reliability arguments as successful reference cases alongside
the degassing and cold-lithosphere omissions. Each fresh capture may use new
record IDs or a different ontology. Compare meaning and source support, not
matching names or total edge counts. Inspect added links for unsupported claims
and report losses as well as gains. Keep subject resolution as a separate case.

Then admit and replay each accepted capture and evaluate the same questions
under review protocol v3.2. Bind the frozen reader to each accepted ontology's
types without seeing its population. Those bindings cannot be presumed byte-
identical across model-created ontologies; record them and separate binding or
retrieval failures from capture failures. Do not modify the reader to rescue
one condition. Semantic assessment remains model-assisted until human ratified.

## Verified coordinates

- Historical Core: `c95dba7b86bb61487bda9a52458e1ea47cce20ab`.
- Correction paragraph: `723a4f92c4a11d695547d21d3bc213ef80a6b503`.
- Historical manifest SHA-256: `db7454fbaeb187442720b1a637b8147a8bd430dca1717ee862c7ffbcf25da5bc`.
- Old skill SHA-256: `8278753a08ff8fdbd867c9da212f160f049378b63d55663ed29e5ce73ccd0a81`.
- Derived skill SHA-256: `c78de1e759a2ede618ac3e35e079fb0cdceb654a42826c37b7b2ebf004615f62`.

Reproduce the read-only inventory with the locked interpreter:
`PYTHONDONTWRITEBYTECODE=1 .venv/bin/python paper-v4/experiment-v4/relationship_contrast.py`.
All seven other inputs have identical bytes across the pair. Nothing here
supersedes frozen captures, review judgements or published Core coordinates.
