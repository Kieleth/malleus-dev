# Sol end-to-end comparison

## Corrected current-adoption condition

E-0245, 2026-09-07. Luis approved "Document and fix and rerun". One new run,
`sol-e2e-corrected-01`, consumes Core
160878cf14c0d27b11a440e26688708e9b7a7e2b, tree
d946a7550dee247a371889f5eadf1cfecc964450. This is the already accepted guidance
correction, not latest main. It supersedes the historical pin below only for
this new condition. All historical inputs, outputs and tests stay unchanged.

The producer is a fresh gpt-5.6-sol agent, explicitly at low effort, with no
inherited context. It constructs its own ontology, then populates it in that
same session. The original two-phase task remains unchanged except run ID and
workspace. A separate administrative delivery appendix requires bounded reads
of the exact inputs and accepted surface. It supplies no source interpretation,
question, answer, record quota or modelling hint. This delivery change and the
restored skill are both disclosed; this run does not isolate skill effects.

Before code: claim is prevention of accepted-guidance rollback and undetected
incomplete input delivery. A regressed packet and a missing or truncated chunk
must refuse; the exact corrected packet and complete delivered frames must pass.
Reuse run-21 preparation and compilation, Core's existing guidance conformance
check, and the current public E2E runner. Exclude Core changes, new extraction
logic, source facts, semantic admission rules and historical rewrites.

Milestones, in dependency order:

1. Record the decision. Add RED guards for old guidance, wrong condition or
   settings, changed or extra inputs, and incomplete delivery. Reuse the exact
   accepted Core guidance test, not a paper rewrite of its meaning.
2. Implement current-adoption staging beside historical replication. Verify
   the installed runtime against the selected tree. Freeze all eight inputs;
   seven remain byte-identical to run-21, only the skill changes. Freeze the
   administrative delivery helper and prompt appendix. Tests must be GREEN.
3. Launch once. Read complete inputs in bounded frames. Verify model-visible
   tool outputs, not a self-reported reading receipt. Return compiler diagnostics
   at most twice, to the same agent. Acceptance requires its own ontology and
   observed model/settings. Bind questions before population is visible.
4. Continue that agent with its own accepted surface and the existing phase-two
   instructions. Verify complete surface delivery. Retain every submission and
   at most two exact structural returns. No semantic feedback or hand repair.
5. Admit, reopen, replay and rerun from empty. Execute the frozen thirty-query
   instrument. Inspect source faithfulness and answer coverage separately from
   structural acceptance. Complete model-assisted review remains subject to
   Luis's human ratification.
6. Record the outcome and source-to-record differences. More relations alone
   are not success. If useful capture improves, report this one changed condition
   without assigning causality to a single factor. If metadata-only capture
   persists, the baseline repair is mechanically complete but does not solve
   semantic extraction. Do not launch a feedback condition without a decision.

Only paper-owned harness and documentation paths may change. Source-bearing
outputs stay private. No commit, push, branch/ref change, manuscript rewrite,
Core mutation or automation restart is included.

## Retained historical condition

Author decision: 2026-09-07, E-0241. Run the complete procedure used by the
Opus captures, not population against an inherited ontology. Two independent
Sol sessions, sol-e2e-01 and sol-e2e-02, each retain their own context through
ontology proposal and population. Existing results remain unchanged.

## Fixed condition

Reference procedure: experiment-v4/run-21. Core remains
c95dba7b86bb61487bda9a52458e1ea47cce20ab, tree
39a9b9da7dcd41660b8b10fb20b1b1d8e2261082. The eight initial input files are
byte-identical to that procedure: skill, selected reading, root ontology,
LinkML types, three optional packs and history profile. No project ontology,
population surface, previous result, question or answer enters phase one.

The initial producer prompt is the retained Opus prompt with only the run ID
and workspace path substituted. The producer model is gpt-5.6-sol, launched
with no inherited conversation. Execution settings are read from the actual
session metadata. No extra minimum relation count, semantic correction prompt,
new stopping rule or source-specific hint is introduced into this comparison.

This matches the two-phase task and artifacts, not the different vendors'
agent environments or undocumented reasoning settings. Opus's effort remains
unrecorded. Do not present this as an isolated model-weights comparison.

## Sequence and gates

1. Freeze and check the eight-input closure and exact substituted prompt.
   The new test must fail for a population-only packet or changed instructions.
2. Each fresh producer proposes its own ontology. The parent uses the retained
   run-21 compiler gate against pinned Core. Return exact typed refusals to
   that same agent, at most twice. No hand repair or inherited ontology fallback.
3. On acceptance, retain its ontology digest, compiled surface and diagnostic.
   Check grounding citations separately. Bind the thirty-question method to
   the accepted surface before population is visible, without answer values.
4. Resume the same agent with the original phase-two dispatch and its own
   accepted files and coordinates. Preserve all submissions and at most two
   typed structural returns. Do not substitute a fresh population session.
5. Execute capture, population-plan compilation, admission, reopen and replay
   through the existing public Core runner. Retain the complete ledger, census,
   graph and traces. Reproduce from empty with identical transaction inputs.
6. Execute the frozen questions against the replayed graph and perform complete
   source-grounded model-assisted review. Report unresolved human ratification.
7. Compare ontology choices, source exposure, assertions, typed records,
   relations, subject links, gaps and answer evidence with the Opus captures.
   Explain omissions through source-to-record witnesses, not just counts.

If the new Sol runs capture relations, that establishes ability under this
end-to-end condition, not that session continuity alone caused the difference.
If they do not, trace the failed stage and producer strategy. Preserve both
outcomes; no run-until-success. New improvement conditions follow the governing
master-plan investigation loop and remain separate from this comparison.

## Ownership and implementation

Paper stages inputs, coordinates agents, binds queries and evaluates. Core
implements generic Core changes only. This run requests no Core change or
rebind. Reuse run-21 preparation, ontology gate and public execution functions;
do not duplicate a protocol machine or author accepted facts. All source-bearing
outputs stay below private/paper-v4-answer-demonstration/sol-e2e-01 and
sol-e2e-02. No PDF, source upload, commit, push or shared-ref change.

The mechanical guard is answer-demonstration/test_e2e_condition.py. It first
failed on both missing packets, then passed after staging the exact eight
inputs. The original preparation function is invoked with each new run's
manifest explicitly bound, preserving the interpreter and digest preflight.
Producer agents: /root/sol_e2e_01 and /root/sol_e2e_02. Both phases, admission,
replay, exact from-empty reproduction and complete independent model review
are finished. 01 compiles at attempt one; 02 compiles after one exact INVALID_RANGE
diagnostic and its own scalar-range correction. Their graphs contain 74 and 81
metadata-only claim entities, no relations and no returned query rows. Reviews
yield 0/0/30 each, with human ratification pending. Both effective model settings
are gpt-5.6-sol at low effort. No semantic repair or additional condition ran.

E-0242 and answer-demonstration/E2E-RESULTS.md retain results and the source-to-record
diagnosis. One producer recovered complete source delivery; the other did not.
Neither inherited an ontology. The missing ontology phase does not explain away
this outcome. The proposed interpretation-clarification probe is not selected.

E-0243, author deferral: park this investigation and its proposed continuation.
The master roadmap's split-producer item now includes cross-model ontology
operation and the possible loss of capture purpose and design rationale at
handoff. No further run is authorized by that todo; it is not a gate for other
work. The hypothesis and both experiment conditions remain distinct.
