# Accepted check-input carrier and bounded preflight

Luis approved the recommendation after `407fbee`: reuse SourceArtifact for the
five retained byte-input roles, with explicit role-specific content validation.
This records that selection for the research OPTIONAL_PROFILE only. It does
not approve an action runtime, public wire, ontology change or shared promotion.

The existing carrier's ID names its record, not an arbitrary ID inside its
payload. Invocation byte references bind that record ID and exact byte digest.
Record references instead bind record ID and record hash. Artifact semantic
hashes retain their existing separate recipe. Any additional relation to an
inner content ID belongs to the explicit event program, not implicit aliasing.

## Bound slice before code

Claim: a research preflight can check that a supplied invocation's ordered
role-input references agree with full typed record wrappers, exact byte payloads and the
selected compiled record contract. It must refuse mismatched IDs, hash domains,
bytes, content types and missing data. No checker is invoked.

Smallest observation, CONFORMANCE_FIXTURE: TYPE and DIRECT_GRANT specimens pass;
swapped identity/byte bindings, missing records, abstract or wrong record types,
malformed contents, and a mismatched TYPE record contract refuse. Both success
and refusal leave the supplied specimen unchanged. The result explicitly says
retention is unverified and runtime did not execute.

Reuse: the approved carrier table, invocation schema, public compiled-artifact
loader, Assent record hashing, SourceArtifact field constructor, and the
existing scope/interval/context parsers. This is research inspection tooling,
not a second runtime resolver. Existing role names stay in the binding data.

Excluded: monitor/implementation binding (the invocation envelope only checks
their reference shapes), actual applied-prefix resolution, time/state currentness, event or
provenance admission, complete static monitor dependencies, producer bytes,
policy control, persistence, source effects, package changes, shared-main or
adopter edits, merge and push. Static consistency cannot satisfy any of these.

Pre-action checks: no server or endpoint; required inputs have no defaults;
the candidate table is replaced by the selected table, with no fallback path;
negative tests precede implementation. Keep the prior candidate report intact.

InputPreflight consumes SelectedCarrierBinding
InputPreflight consumes SuppliedRecordAndByteSpecimen
InputPreflight produces StaticConsistencyResult
OwningHistory alone establishes AppliedPrefixMembership
FutureEventProgram governs ContentIdentityAndClosureAssociations
