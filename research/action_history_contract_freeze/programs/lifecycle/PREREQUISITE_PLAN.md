# Prerequisite registration definitions

Base: `016d046`. Luis approved continuing prerequisite definitions after the
canonical-list addition. This is OPTIONAL_PROFILE research definition work;
the tests are CONFORMANCE_FIXTURE observations. No runtime is authorized.

Claim: define bounded full-record grant, monitor and policy registration
specimens, preserving existing Assent record and semantic-hash identities.
Use the selected twelve instructions only. Catalogue any remaining gap
instead of adding a primitive or narrowing the claimed registration language.

Smallest observations: full specimens validate against compiled Assent;
declared metadata, hash and applied dependency paths pass static checking;
declared semantic preimages agree with the existing control digest functions;
dependency paths name the required hash category and missing operands refuse
statically. A digest-shaped value alone cannot distinguish record and artifact
identity. Path assertions and distinct-value hash witnesses check that binding;
the future runtime must verify the actual referenced records. Real control
helpers distinguish malformed values independently of static success.

Reuse: source registration's shared prelude and global-ID target, complete
policy shapes from the context/proposal packet, record/value hash recipes,
RESOLVE_RECORD, membership, canonical-list checks and scalar comparison.
The exact semantic preimage is a declared input whose fields are compared
to the record before HASH VALUE. No callable or new projection language is
introduced. The test-side field mapping is definition metadata, not a runtime
producer.

The bounded variants are a finite-ended grant, a two-input monitor, and
two-monitor epistemic and authorization policies. Counts are conformance
specimen choices, not restrictions imposed on Assent or consumers. Other
arities and absent/null end variants remain uninstantiated. Real monitor
implementations and policy choices remain UNBOUND.

Known shared gap: the current compiled STRING shape and finite instructions
do not express Assent's whitespace-aware nonblank check. Preserve an exact
grant witness, and explicitly retain the same obligation for monitor/policy
identity fields. Do not hide it in VALIDATE_RECORD, minLength, sorting,
regex, HASH VALUE or a new capability name.

Pre-action: no server, endpoint, installation, required-data default or
production replacement. RED commit precedes data. Exclude source effects,
authenticated history resolution, instruction execution, event append,
complete registration conformance, shared main, governance, packaging,
paper/adopter edits, merge and push.

RegistrationProgram consumes FullAssentRecord
RegistrationProgram consumes ExactSemanticPreimage
RegistrationProgram requires AppliedDependencyRecordHashes
StaticValidator conformsTo ExplicitOperandClosure
RegistrationProgram requires UnresolvedNonblankValidation
