# Atomic context and proposal definition

Status: authorized research definitions only. No runtime activation.
Base: `e79a3362165e567674fe985fc2c25aabd9a47cab`.

Claim: OPTIONAL_PROFILE. Make the retained-input origins and one first-revision,
one-action context/proposal specimen inspectable as data. Reuse the eleven
approved instructions, existing full record shapes, canonical identities and
the accepted two-event transaction. Tests are CONFORMANCE_FIXTURE observations
of static definitions, not evidence of execution or atomic persistence.

Smallest observation: the specimen passes the existing static validator; its
record shapes accept real compiled Assent records without dropping fields.
Its coordinates cannot exchange domain and action heads; its only state
targets are named protocol indexes. Existing inputs resolve from the applied
prefix, while the new context can resolve from earlier staged introductions.

Pre-action checks: no server or endpoint, no required-field defaults, no
production mechanism replacement, no installation. Missing data fails the
definition gate. No new instruction, parser, executor or authoring language.

The finite specimen selects absent optional predecessor/candidate fields,
revision one, empty claim/evidence members and one concrete LocalAction test
type. It is not the complete set of valid record variants. The full lifecycle
remains governed by `../LIFECYCLE.md`.

Sequence: commit failing definition tests, add the data, run focused and
existing compatibility checks, then record exact commits and non-claims.

Excluded: resolving real history, authenticating supplied replay dictionaries,
running checks, committing either event, action authorization/execution,
domain graph writes, production/public changes, package work and promotion.

RetainedInputDefinition consumes ExistingInputBindingAndInvocationContracts
ContextProposalProgram governedBy ApprovedAtomicPair
ContextProposalProgram consumes VerifiedPretransactionHistory
FutureInterpreter conformsTo ContextProposalProgram
