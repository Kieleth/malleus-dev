# Initialization and prerequisite declarations

Status: authorized definitions only. Base `e5ff2a7`.

Role: OPTIONAL_PROFILE for the one-history action definition. The observations
are CONFORMANCE_FIXTURE tests, not runtime conformance.

Claim: express the initialization checkpoint and source registration using
existing instructions and complete record shapes. Catalogue the remaining
registration obligations without promoting lexical or shape checks into
verified history. Initialization must be once per owning history, bind its
actual prefix and selected dependencies, set A to the checkpoint content
identity and leave domain state unchanged.

Smallest observations: static program/type checks, real compiled SourceArtifact
shape compatibility, and exact equality between the declared source semantic
hash projection and the existing source-artifact digest function. Preserve an
exact witness if an existing requirement cannot be expressed.

Reuse: approved initialization schema, context/proposal data shapes, eleven
instructions, source and record hash recipes, existing canonical-list checks.
No instruction or runtime implementation changes. No new artifact grammar.

Pre-action checks: no server/endpoint, missing data refuses, no production
replacement or fallback, no installation, RED before definition data. Actual
history resolution, instruction execution, producer execution, appends,
effects, promotion, shared main and governance are excluded.

Known closure question: existing grant and monitor registration require
canonical string-list ordering. Uniqueness is not ordering; sorting an input
would change retained bytes. Record the exact unsupported ordering attempt
without adding an opcode or silently narrowing those lists to one item.

InitializationDefinition governedBy AcceptedCheckpointIdentity
SourceRegistrationDefinition consumes ExistingSourceArtifactProjection
RegistrationDefinition requires CanonicalDependencyAndPermissionLists
FutureReadOnlyPreparation conformsTo CompletedDeclarations
