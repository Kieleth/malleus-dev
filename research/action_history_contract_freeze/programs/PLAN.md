# Action program closure

Status: research-local definition and tooling, not runtime authorization.
Base: `4cf8efe591c819fef24a325839162109d0cefdce`.

## Bound slice

Role: OPTIONAL_PROFILE for the one-history action definition. The validator and
its synthetic examples are a CONFORMANCE_FIXTURE, not an interpreter.
The approved supplier experiment is the concrete consumer. No shared promotion
or second-implementation claim is made.

Claim: make the event-program obligations inspectable and check their static
operand, dependency and state-target closure before attempting execution.
Close referenced interval, equality-scope and current-context contents and the
monitor invocation/output contract. The selected original-context/proposal pair
stages in order and commits once; no orphan context is permitted.

Smallest observation: a finite program specimen passes static validation;
undeclared or mistyped operands, forward results, repeated outputs, missing or
cyclic introductions, unknown capabilities and domain-state targets refuse.
Concrete content specimens exercise required fields, time ordering and exact
reference identity. Missing opcode attempts remain failures of program closure,
not silently added operations or skipped runtime tests.

Reuse: the preceding ten-instruction JSON Schema, compiled Assent record
contracts, existing canonical and record hashes, explicit head/count, policy
evaluation and atomic staging. Use the configured environment without installs.

Excluded: executing instructions, actual TYPE/direct-grant producers, monitor
invocation, event append, trusted replay construction, source effects, supplier
code, new production API/grammar, packaging, shared main, merge and push.

Pre-action checks: no server interaction or endpoint; required data must refuse
when absent; no replacement of production mechanisms; no fallback or callback;
negative tests precede implementation. New instruction semantics require Luis.

## Sequence and evidence

1. Commit static/content RED tests before the research validator.
2. Implement only pure definition checks and record real GREEN results.
3. Publish the complete lifecycle requirement/program map and monitor contract.
4. Classify each obligation as statically checked, missing instruction, or
   unexecuted runtime conformance. Never count the latter as passing tests.
5. Return any genuinely new instruction choice with an exact witness.

StaticValidator consumes CandidateProgramAndClosedInputSchemas
StaticValidator consumes FiniteInstructionSchema
StaticValidator consumes DeclaredProtocolTargets
StaticValidator produces DefinitionDiagnostics
ProgramDefinition governedBy AtomicContextProposalDecision
FutureInterpreter conformsTo ReviewedProgramsAndRuntimeCorpus
