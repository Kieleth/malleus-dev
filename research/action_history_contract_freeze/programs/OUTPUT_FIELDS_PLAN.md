# Check-output field closure

Status: approved continuation of the research definition, not runtime work.
Base: `ba058d2b8505a6ac4648b1d65cb9b95af5891b19`.

Claim, OPTIONAL_PROFILE: make every field of the selected TYPE and direct-grant
assessment/failure variants traceable to explicit metadata, resolved inputs,
computed results, or the existing record-hash operation. This refines the
already recorded `MONITORS.md` contract. It selects no new protocol semantics.

Smallest observation, CONFORMANCE_FIXTURE: data-driven specimens of all six
record variants satisfy the actual compiled Assent types. Tests independently
check required-field coverage, distinct identity domains, the two acceptance
heads, computed-result ownership, and the failure pair's shared bindings.
Removing a required field must fail real record validation. These are record
shape and definition tests, not producer, admission, or replay tests.

Reuse: compiled Assent, `record_hash`, the explicit invocation envelope, and the
existing monitor contract. Paths in the field census are documentation data,
not a new public grammar or an instruction added to the runtime.

Excluded: producers, input resolution, new artifact kinds, lifecycle execution,
ledger writes, policy evaluation, external effects, packaging, shared checkout,
shared governance, merge and push. Actual retained input wrappers, closure
ordering, and reviewed producer bytes remain UNBOUND. Test witnesses cannot
satisfy those obligations. Missing carriers must not be disguised as another
artifact kind merely to make a specimen work.

Pre-action checks: no server or endpoint; missing fields fail, no defaults;
no production mechanism replacement; no callback or new instruction. Record
RED before adding the field census, then run the focused and existing combined
research/compatibility checks. Preserve predecessor evidence.

OutputFieldCensus refines MonitorOutputContract
OutputFieldCensus consumes ExistingAssentRecordTypes
OutputFieldCensus consumes ExplicitInvocationMetadata
OutputFieldCensus identifies UnboundComputedAndClosureInputs
FutureProducer governedBy ReviewedMonitorOutputContract
