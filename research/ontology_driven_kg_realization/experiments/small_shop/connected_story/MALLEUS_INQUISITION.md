# Connected Shop self-check

Scope: the new ontology-led Table 1 consumer only. This is not an audit of Core,
the paper, or the frozen predecessor Shop examples. Rubric: packaged v12,
`6509f1b8fce1...`, read in full for the applicable judgment rites.

## Mechanical result

At implementation `90c3aeee`, the exact public schema command with absolute
import mappings returns:

> ROOT ONTOLOGY PROFILE PURITY SEAL GRANTED. The schema may serve.

No rites are disabled. Notes: no Signal subtype, no ordinary Relation subtype,
and no Agent mixin. These are deliberate boundaries: this run uses qualified
EventParticipation, no computed signals, and no actor authorization. They are
not claims that those types are unnecessary for every Shop use.

The first invocation used relative import-map targets incorrectly. The command
refused because those resolve against the schema directory, not the shell's
working directory. The corrected command uses resolved public bundled paths;
`test_documented_schema_check_uses_resolved_import_paths` executes it and checks
the verdict. No parser, severity or Core source was changed.

## Judgment

- **Closed, authority boundary:** Shop vocabulary and selected state restriction
  are adopter choices. Core owns compilation, atomic admission and replay.
  [`run.py`](run.py) imports no private Core code and writes no accepted graph
  directly. It never authors successful check outcomes.
- **Closed, source and identity:** the source inventory verifies the retained
  image, rows and context; plans name exact retained inputs and field locators.
  The integration checks all 21 rows, 123 nonempty fields and every historical
  record's trace. The producer is tested with answer-key reads forbidden.
- **Closed, state meaning:** [`shop.yaml`](shop.yaml) declares one abstract state
  category with two concrete families. The program reads that category through
  the exact profile. Occurrences and physical units remain outside it. The
  adapter refuses an update without one same-order/product predecessor; Core's
  category guard is not advertised as a universal same-owner identity rule.
- **Closed, honest derived results:** object joins are read from qualified
  occurrences. [`mapping.json`](mapping.json) owns the explicit interpretation.
  The report returns `NOT_EVALUATED` for shipment eligibility rather than
  treating absent payment data as an unpaid balance.
- **Open, source faithfulness:** transcription has a retained visual reference
  but human ratification is pending. Field accounting, successful compilation
  and exact replay do not establish complete or correct source interpretation.
- **Open, remaining story:** the context's customer/payment condition has not
  been evaluated, source-relative timelines remain unselected, and this run
  neither authorizes nor executes shipments. These are named next deliverables,
  not silent runtime fallbacks.
- **Scoped reuse:** the public compiled contract, history program and profile
  are reused without promoting Shop into Core. This is a bounded source mapper,
  not a generic frontend or proof of another interpreter.

## Shipment reader successor

The earlier notes above describe the frozen population. Shop-only reader
`shipment_explanation.py` now joins the retained explanation and rule to real
accepted graph checkpoints through public `graph_at_change`, queries and trace.
It checks explicit unpaid-count bounds, not guessed invoice status. The source
still does not establish a complete customer account, so actual eligibility
remains unknown. No source, mapping, ontology, admission policy or Core path
was changed for this reader.

The tests cover missing invoice/receipt/link evidence, wrong ownership,
unchanged history and graph, source identity refusal, and synthetic explicit
status controls. Known clearing is separate from a complete balance snapshot.
This closes the pending bounded source/payment explanation, while domain
ordering, human source ratification and action authority remain outside it.

## Object-reader successor, 2026-09-14

This is a focused reader check, not another schema audit. The acolyte skill was
reread in full before work. No ontology or Core path changes in this slice.

Luis explicitly selected the chapter as trusted input for this experiment and
its correction as the accepted successor. External source verification is not
a blocking gate. That assumption supersedes the earlier human-ratification
requirement for continuing this demonstration; it does not fill absent values.

`object_timelines.py` reads one accepted replay through public APIs. Its
specification names the printed-coordinate convention and the absence of
calendar, duration and causality claims. It keeps shared occurrences singular,
ties unordered and unusable times unplaced. Order scopes are retrospective
recorded joins, not ownership backdated before the relating occurrence.

The tests exercise source substitution refusal, exact participation/occurrence
traces, stable views under reversed graph iteration, read-only CLI and reopen.
The integrated run compares full and maintained views after every admission
and the existing shipment explanation at the selected three checkpoints.
Final test evidence belongs in `VALIDATION.md`; no broad correctness claim
follows from this focused check.

Remaining comparison boundaries: full directly-follows behavior under missing
times, warehouse enrichment, delay measurements and the chapter's executable
analysis have not been reproduced here. These are later Shop work, not inferred
Core defects. No external source-checking mechanism has been added.
