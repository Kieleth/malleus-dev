# Producer-bound Shop evidence, 8 September 2026

These outputs were captured from five fresh histories at `f65a5ec`, using the
unchanged current compiler and existing Shop runners. `binding.json` names
every complete validated compiler artifact, its producer, each output digest,
and each predecessor file. It is test data, not a new protocol grammar.

The current evidence assertions verify the compiler artifacts from retained
history, including the target of an ontology revision, before comparing the
complete output bytes. They never regenerate expectations, discard a digest,
or select another producer. Changing the compiler calls for inspection and a
new recorded generation, not an automatic update.

All ten predecessor files remain byte-identical. The earlier evidence archive,
original published receipts and fixed two-producer compatibility gate are also
unchanged. The recorded `changed_paths` describe this exact transition only.
Tests verify that they are all and only the changed SHA-256 fingerprints, with
no changes to values, records, source data, operation fields or time semantics.
Both graph files are byte-identical to their predecessors. Live integration
tests additionally check independent source, graph, correction and trace facts.

The five cases are correction, object-event participation, public population
with a contract revision, the replayed-query showcase, and fresh synthetic
supplier rows. These are separate conformance histories for their selected
profiles, not five competing authorities for one domain state. The sequential
action tests separately compose two actions and a Shop correction in one log.

No runtime, ontology, package, downstream or source dataset changed. External
effects and observations in the action proof remain synthetic. Exact old
receipts make claims about their original producer, not today's output bytes.
