# Semantic Re-entry overnight continuation

Historical plan. The current workspace, Core gate and execution state are in
[INTEGRATION_STATUS.md](INTEGRATION_STATUS.md). The existing continuation now
runs every five minutes with no overnight cutoff and continues implementation
after the verified Core gate. The old schedule and paths below are retained as
history, not instructions for the active continuation.

Operator instruction: sustained work during the night of 7 September 2026,
toward an inspectable E2E slice. This thread owns vision, experiments and
consumer conformance. Core owns Core implementation. Robotics is a related
consumer to inspect, not a replacement for the Small Shop proof.

This plan coordinates work. It is not a protocol artifact, acceptance decision
or new public contract. The experiment designs remain ADOPTER_CHOICE; tests and
retained observations are CONFORMANCE_FIXTURE under their selected profiles.

## Start here on each continuation

Read this plan and its progress section, then inspect active local runs and
compact updates from the two owner tasks. Do not repeat a completed run against
unchanged inputs. Check exact files and identities before consuming a handoff.

- Re-entry worktree: `/private/tmp/malleus-reentry-action-design.drq44c`.
- Core task: `01a02f71-fec6-7382-9c68-c3efd3dba5d4`.
- Robotics task: `01a079a5-4cfe-71e2-b4ab-33791c0d9078`.
- Continuation: `malleus-re-entry-overnight`, this thread, every 30 minutes,
  ending at 08:00 America/Los_Angeles on 7 September 2026.

The continuation's initial creation refused an omitted thread target. The
corrected request explicitly bound this thread, and both the app view and saved
configuration confirmed ACTIVE with the bounded end time. Do not create another
continuation if this one is already present.

## Verified baseline, not a current execution claim

[COMPILER_GATE_CHECK.md](COMPILER_GATE_CHECK.md) and its JSON retain the completed
compiler prerequisite. Consumer commit `f221c0994df530170c80726be29ff876d3dd195e`,
tree `2bad91898a27618107997c526f9cbc99cbaf5c2b`, was clean. Core evidence-only
handoff `ff0e1b31f3b44be56a63465b9180d7bbf4c17e54`, tree
`b02650464120d2f43ba28c4613b9c01d7f8d6806`, agrees with the independent run.
The bounded gate passes. The raw repaired suite remains 1165 passed, nine exact
historical receipt mismatches and one existing xfail. Thirty-six focused gate
and actual compiled Assent checks pass.

The compiler blocker described in the older frozen FREEZE_GATE.md has therefore
been resolved. Do not treat that historical document as the current blocker,
and do not rewrite its old observations. Action grammar and implementation
remain a separate boundary.

The existing internal-correction experiment uses already-retained e7 evidence.
It proves its bounded source-backed KCS path, not that a supplier action caused
new bytes. The separately frozen supplier fixture has initial e4 only and an
expected new occurrence under `oracle`. That oracle must never become a capture.

## The actual target

The smallest decisive observation is one fresh run of this sequence:

```text
accepted B/Y/1
  -> contract-local goal: commitment equals 2
  -> pure, pinned ActionProposal
  -> normal acceptance, authorization and dispatch
  -> one controlled synthetic supplier-file attempt
  -> independent capture of the actual resulting bytes
  -> source-to-existing-KCS proposal at fresh state
  -> ordinary acceptance and JSONL-only replay
  -> accepted B/Y/2, unchanged complement, no further candidate or write
```

Reuse the frozen case, CORE_CONTRACT.md, ADOPTER_CONTRACT.md and existing public
compiler/effect semantics. Do not invent accepted demand, reuse e7 as new
observation, introduce a second change identity or give synthesis a graph writer.
Only accepted KCS application changes the domain graph. An execution receipt,
observation record or pending action alone must leave accepted quantity at one.

This is a synthetic source commitment goal. It does not prove supplier delivery,
customer fulfilment, physical robot safety, source truth or effect causality.

## Dependency order and ownership

1. Core completes the currently approved finite action-contract definition:
   initialization, original context, exact digests, typed instructions and
   capabilities, check specifications and input bindings. The TYPE/direct-grant
   producer implementations do not exist. Their identities stay unbound until
   actual code exists. Specification hashes cannot impersonate implementations.
2. This thread reviews the exact handoff against the frozen semantic contract
   and supplier oracle. Return one dependency-closed discrepancy report. If the
   grammar introduces a new semantic or architectural decision, obtain Luis's
   decision before the affected implementation. Persistent work is not consent
   to choose those meanings on his behalf.
3. Freeze consumer RED cases against the approved contract. Core owns any
   required generic implementation and its class-level guards. Re-entry owns
   the domain synthesis, model and experiment obligations. Use only actual,
   verified Core interfaces; unavailable production seams remain explicit.
4. Once the implementation gate and authority are satisfied, compose the pure
   synthesizer, controlled adopter executor, independent observer and observed
   source mapper. Each implementation is explicitly identified and selected.
5. Run the real public-seam loop, its hard refusals, replay and quiescence.
   Record failures, then fix the defect class and rerun only affected inputs.
   Finish with the relevant unified gate, clean isolated commits, diff audit,
   commit/tree and file hashes, and a visible explanation of the recorded run.

No merge, push, publication, paper edit, paid call, physical effect, large
download, model/backend selection or change in another task's worktree is
authorized by this plan. No general planner, queue or alternate ledger is needed.

## Work that remains useful while Core defines the contract

Use the existing level-by-level thought experiment in README.md instead of
duplicating it. Turn each proposed case into an explicit input, observation and
refusal obligation. Preserve the separation between canonical serialization,
log replay and typed putback. External action-model simulation is not PutGet
about the world.

The existing internal tests already identify concrete obligations for immutable
contract round-trip, unsupported policy/operator, ambiguity, budget, complement,
stale-before-satisfied checks and no-op. They live in test_consumer.py,
test_integration.py and test_guardrails.py under research/semantic_reentry_protocol.
List actual files before selecting tests. Their old evidence remains frozen;
the new action composition needs its own execution evidence.

Prioritize the unproved external boundaries: own-lifecycle log advancement
versus unrelated state changes; pending versus satisfied; success receipt with
unchanged source; failed attempt without capture; failed receipt after a real
write; observed but refused KCS; exact fresh-head no-op. Do not add xfails that
make those missing stages look implemented.

[ROBOTICS_REENTRY_MAP.md](ROBOTICS_REENTRY_MAP.md) records the first code-level
cross-domain audit. Continue it only when a new retained robotics boundary is
available. Keep its prefix isolation, assessment revision and fixed reader
separate from the still-future action/controller seam.

## Binary finish rule

Full external Re-entry E2E is proved only when the complete recorded run above
crosses the real Core boundaries and all applicable negative cases pass. The
visible artifact must distinguish actual source, expected oracle, accepted KG,
proposal, execution receipt and observation at every step. No modeled or
hand-built accepted state can substitute for the missing application/replay path.

At the morning cutoff, report the strongest result actually obtained, the exact
unproved boundary and any required decision. A precise remaining blocker is
better evidence than a simulated runtime labeled as complete. Keep the loop
quiet on unchanged state, and do not duplicate requests already sent to owners.

## Progress

- Successor membership/array packet `7f2f850`, tree
  `566b2e8a523aa3cc22da178b8c6462447a110b38`, closes SV-1 after 45 independent
  focused passes. CORE_PROGRAM_REVIEW.md retains the exact successor review,
  hashes, guard and regression evidence. No new defect was found in the bounded
  delta. The instruction is defined and statically checked, not executed.
  Core's 231-test result remains owner-reported. The transaction and membership
  decisions are closed; full JSON lifecycle programs and runtime are not ready.
- Successor program packet at Core `fdb4972` is independently reviewed in
  [CORE_PROGRAM_REVIEW.md](CORE_PROGRAM_REVIEW.md): 55 focused checks pass,
  but diagnostic SV-1 finds unsupported positional-array schemas can falsely
  pass scalar operand typing. Core owns the requested class guard and hard
  regression. Full event programs and runtime remain absent. Luis has approved
  bounded STRING membership; the frozen packet's pending-choice wording is
  historical, and Core is preparing its separate successor. Neither that
  approval nor the static test count establishes executable readiness.
- Successor update, 7 September: R1 is CLOSED. Luis selected one ordered,
  failure-atomic context-registration/proposal transaction: both commit or
  neither does. No intermediate committed context, unrelated intervening
  event, separate-commit variant or orphan recovery is selected. This
  supersedes the earlier open-choice statements below and in the 07:15
  MORNING_HANDOVER.md snapshot, not their historical evidence.
  Core decision commit `4cf8efe591c819fef24a325839162109d0cefdce`, tree
  `a3cad91a3280057afd2f41f9d4bd94a1c90983b9`, was read and independently
  checked. Its four-file delta from `ae434b5` is definition/fixture only;
  earlier definitions and runtime remain unchanged. TRANSACTION_DECISION.md
  SHA-256 is `a8fbe0bf0eb840499b4e89ab354caa28e90f31010950ca2f3e0073b03f9fd4fe`.
  All 19 transaction-definition tests pass independently in the clean exact
  clone `/private/tmp/malleus-reentry-transaction-audit.G90UeU/repo`, with
  runtime-origin/HEAD preflight and explicit Core pytest configuration. The
  sibling `independent-focused.xml` has SHA-256
  `6ed8efb04c85e6d42a63b208abc24b2be8143439be3f0ffac2c7538749667dd2`.
  Core reports 159 combined passes and retained RED `dd23691`; neither that
  full selection nor RED was rerun here. No behavioral transaction, rollback,
  producer or lifecycle result follows from these definition checks.
- Morning evidence snapshot at 07:15 America/Los_Angeles is consolidated in
  [MORNING_HANDOVER.md](MORNING_HANDOVER.md). It rechecks the six-file overnight
  diff, exact ledger and JUnit hashes, and unchanged owner coordinates without
  rerunning gates. The watch still ends at 08:00. Only a material later handoff
  requires an update; the snapshot does not claim observation through cutoff.
- 2026-09-07: overnight continuation created and verified for this thread.
- Core informed of the overnight objective and ownership. It confirmed work
  remains contract definition, with absent producer identities explicitly unbound.
- Robotics source audited at `f1402ca`; live uncommitted pilot work was excluded
  from frozen claims. No robotics test or inference was run by this audit.
- First concrete cross-domain distinction: Shop equality-to-two is not the
  robotics reader's at-least-two stop condition. Duplicate assessment identity
  no-op is also not goal-satisfaction quiescence.
- Completed [replay non-invertibility witness](REPLAY_NONINVERTIBILITY.md): two
  independent, JSONL-only replay-valid histories contain 28 and 29 events but
  the same current B/Y/2 graph and accepted KCSs. Two focused tests and 112
  relevant checks pass. An initial wrong-checkout import was excluded and now
  refuses mechanically before fixture setup. This is not an external action.
- Core reconciled Robotics' older compiler refusal with the existing repair.
  Robotics' exact three-source import now has a verified on-disk success receipt;
  its immutable artifact handoff was still pending at that update. No robotics
  runtime rebind or new action execution followed from that check.
- Robotics subsequently committed its pilot at
  `5727cc1b97aba1de6060684774c9bef4cf11032f`, tree
  `b994c0cc2a906735270412976845c0a3ba535dd5`. Its handover was read completely
  and its handover, result and compiler-probe file hashes checked here. It is
  assessment/replay evidence, not an integrated action consumer. See the
  retained-boundary appendix in ROBOTICS_REENTRY_MAP.md.
- Paper independently verified the replay witness at `51bb72a`, matched the
  retained identities and reproduced its two focused tests against guarded
  Core `2af45e0`. This is a reported independent check; it did not rerun the
  112-test selection and changes no claim about external action.
- Added a conversation-local comparison of the recorded 28/29-event histories.
  The selected log head and receipt change; the full graph digest remains
  identical. It is an evidence display, not a protocol simulator. The existing
  frozen expected action sequence stays unchanged. Files are under the
  thread's writable visualization directory, not Core or the runtime:
  `reentry-observed-replay.html`, SHA-256
  `0028af2dbcafd5e5ffec3ab0155155b259e9d02f96ae3ce1f3f46bc7b3541151`,
  and `test-reentry-observed-replay.cjs`, SHA-256
  `062f6a073c7c16f93358b20fe33171ec1a6db5c411d4ff0a9ca76ae8d2e1d37a`.
  The Node built-in-only check binds the exact retained result file and tests
  selection, absent evidence fields and missing elements. It passes. Run it
  with the configured Node interpreter and the absolute retained
  `replay-noninvertibility-result.json` path as its sole argument. The bundled
  renderer accepted the fragment. Browser inspection of its local-file preview
  was refused by browser policy; no workaround was attempted and responsive or
  theme QA is not claimed.
- Core definition at `63e1c3b`, tree `f59d998bb0c22dfaaf694c344d53c77db0f42085`,
  is now independently reviewed in CORE_DEFINITION_REVIEW.md. Exact bytes
  match; the 140-test focused selection passes independently. No new runtime
  contradiction was found in the stated shape-only scope. Three synthetic
  diagnostic counterexamples confirm declared gaps, not repaired defects.
- Core resolved review note N1 at
  `ae434b5b54b4d1f76440586fddb270b0543a09fe`, tree
  `7beabc42555fe0a8a7bb8875b82f5f9a89a13569`. The four-file delta from `63e1c3b`
  changes definition/report wording, the readiness assertion and byte bindings
  only. Three targeted readiness/binding tests pass independently in
  `/private/tmp/malleus-reentry-readiness-audit.bfd2NY/repo`; JUnit is the
  sibling `targeted.xml`. Exact loaded runtime and clean HEAD were checked.
  Ruff, format and diff pass. The previous 140-test observation remains pinned
  to `63e1c3b`; it was not rerun here or transferred to the successor.
  New SHA-256 values: DEFINITION.md
  `7f809cbd51bb7443b537e16f5b6df32b9471f041efae236e4691525065ca8a72`,
  DEFINITION_REPORT.md
  `d9d68bb56fe07b2016c1ce3c9c5ef37b5359d3915fd1c507c31f1a41edd472e9`,
  definition-inputs.json
  `b94b23196d96c88efc055ebeb3b8d2bc32aff78e1470f032cf421e85b87f9219`,
  and test_definition.py
  `367a3835d67eaf2350236ed4b8c17af252b38e412b8a8ff05d4d91238d6baea0`.
  Missing implementations now explicitly block executable GREEN, not the
  first failing test. Adjacency, transaction grouping and orphan-registration
  behavior remain undecided. No runtime work was introduced.
- Robotics retained a controller-accounting audit at `c968bd3`, tree
  `ec50e79e5ab49f57d76f98c544b3e41d64cf395b`. This thread read the audit and
  verified its manifest, five local source identities and result. No test,
  witness, model or rollout was rerun. ROBOTICS_REENTRY_MAP.md records the
  narrow consequence: removing an error from a resumable summary must not
  restore a supplier episode's exhausted one-attempt budget. This reinforces
  the existing contract and adds no Core requirement or runtime permission.
- Next: Core continues the already-authorized complete event programs,
  research-local static validator and content/check-producer contract closure
  under the selected atomic pair. Re-entry reviews the exact successor packet.
  The transaction choice is no longer a blocker and must not be reopened.
  The eleven-instruction vocabulary remains a candidate until those programs
  demonstrate sufficiency. Executable action-runtime activation still requires
  the reviewed boundary and operator authority. Actual implementation identities
  are required before successful execution, never fabricated to make RED or a
  definition fixture look executable.
