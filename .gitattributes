# Pinned data artifacts keep their committed bytes in every checkout. Their
# identities do not depend on this rule, but a byte-for-byte checkout keeps
# file digests recorded in the governance ledger reproducible on Windows.
/src/malleus/authorization-control-v1.json text eol=lf
